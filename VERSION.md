# Заказ заготовок для ЦМО

## v2026.07.27.3-dashboard-library-import-fix — 27.07.2026 14:18

- На главной панели исправлены актуальные счетчики: `Деталей` считается по активным строкам библиотеки без архивных карточек, `Заготовок` - по активным строкам НСИ, `К закупке` - только по последнему расчету; карточка `Позиций 1С` убрана.
- Карточка `Дубли НСИ` теперь открывает вкладку `НСИ` с включенным фильтром `Только дубли`; этот фильтр также доступен в самой вкладке НСИ.
- Импорт библиотеки теперь распознает 10-колоночный файл вида `IPS / Обозначение / Наименование / Заготовка / УТ-код / Материал / ГОСТ материала / ГОСТ сортамента / Ед. / Количество`, поэтому пример `1410079` обновляет актуальную заготовку `Заготовка 50х105х1630мм Сталь 30ХГСА`, код `УТ000013591`, единицу `шт` и количество `2`.
- При явном импорте библиотеки очищается старый след `Удалено из библиотеки ...` в источнике детали, чтобы удаленная архивная строка не выглядела как актуально восстановленная запись.
- Добавлены регрессионные проверки счетчиков главной, фильтра дублей НСИ и нового формата импорта; `dotnet test BlankDemandPlanner.sln` прошел, 70/70 тестов.

## v2026.07.27.2-msk-drawing-click-open — 27.07.2026 15:35

- Во вкладке `МСК` открытие PDF-чертежа теперь запускается отдельным кликом по строке/ячейке, а не зависит только от смены выделения при наведении.
- Если автоматический поиск при наведении уже пробовал IPS и не открыл чертеж, клик по строке выполняет повторную принудительную попытку через `IPS Bridge 3.3.1`.
- Добавлена регрессионная проверка, что команда открытия чертежа повторно обращается к bridge при клике; `dotnet test BlankDemandPlanner.sln` прошел, 68/68 тестов.

## v2026.07.27.1-msk-ips-drawing-auto-open — 27.07.2026 15:10

- Во вкладке `МСК` из правой панели `Информация по детали` убрана верхняя сводная строка с табличными значениями; карточка теперь сразу начинается с подробных полей `IPS`, `Обозначение`, `Наименование` и далее.
- Из основной таблицы `МСК` убрана колонка `Файл`; путь к исходному Excel МСК остается в детальной карточке и в команде открытия.
- Подключен read-only клиент `IPS Bridge 3.3.1`: при наведении на строку МСК приложение ищет PDF-чертеж по IPS через MCP bridge, сохраняет найденный файл в локальный кэш `%LOCALAPPDATA%\BlankDemandPlanner\IpsDrawings` и автоматически открывает PDF.
- Параметры IPS Bridge читаются из `config.toml`/`ips_bridge_config.toml`, переменных окружения или внешнего контура `Интеграция с сервисами`; секреты не записываются в репозиторий и не выводятся в статусах.
- Добавлена регрессионная проверка карточки МСК без сводной строки; `dotnet test BlankDemandPlanner.sln` прошел, 67/67 тестов.

## v2026.07.24.6-msk-import-nsi-usage-strict — 24.07.2026 15:35

- Исправлен импорт `Данные из МСК.xlsx` для фактического 11-колоночного формата: IPS, обозначение, наименование детали, вид, полное наименование заготовки, материал, УТ-код, ГОСТ материала, ГОСТ сортамента, единица и норма расхода.
- Для примера `1320756 / КПТ-04.00.001А01` импорт теперь берет заготовку `Круг D80 Сталь 40Х ГОСТ 2590-2006 / ГОСТ 4543-2016`, код `УТ000022657`, единицу `пог. м` и норму `0,065`, а не подставляет `Круг` и `1`.
- Окно применяемости в НСИ теперь показывает только детали с активной связью именно на выбранную заготовку, без похожих деталей по материалу, размеру или названию.
- Добавлены регрессионные проверки нового формата МСК и строгой применяемости НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 67/67 тестов.

## v2026.07.24.5-nsi-blank-import-restore — 24.07.2026 15:05

- Уточнено восстановление после импорта МСК: если УТ-код заготовки был перенесен в архив НСИ, импорт `Данные из МСК.xlsx` теперь возвращает в работу и алиас НСИ, и саму каноническую заготовку.
- При повторном использовании нормализованной заготовки импорт также активирует ранее архивированную каноническую позицию, чтобы она снова участвовала в поиске, подсказках и строках библиотеки.
- Добавлена регрессионная проверка импорта МСК поверх архивной позиции НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 66/66 тестов.

## v2026.07.24.4-library-import-restore — 24.07.2026 14:45

- Исправлено восстановление `Библиотеки` после удаления всех строк: повторный импорт `Данные из МСК.xlsx` теперь снимает архивную метку с ранее удаленных IPS и возвращает строки в список.
- При открытии `Библиотеки` добавлено самовосстановление уже импортированных строк: если у архивной детали появилась активная заготовка, карточка снова отображается без повторного импорта.
- Добавлены регрессионные проверки импорта МСК поверх архивной детали и загрузки библиотеки после уже выполненного импорта; `dotnet test BlankDemandPlanner.sln` прошел, 65/65 тестов.

## v2026.07.24.3-library-delete-fix — 24.07.2026 14:20

- В "Библиотеке" исправлено удаление через правую кнопку мыши: правый клик теперь выбирает строку под курсором, а команда `Удалить` идет через общий механизм удаления строк.
- Исправлено удаление выделенных строк в режиме ячеечного выделения: команда продолжает собирать строки из выделенных ячеек и корректно архивирует выбранные объекты.
- Исправлен случай деталей без активной заготовки, у которых уже есть старые архивные карты заготовок: такая строка теперь действительно убирается из библиотеки и отвязывается от строк потребности.
- Добавлена регрессионная проверка удаления детали без активной заготовки при наличии неактивных карт; `dotnet test BlankDemandPlanner.sln` прошел, 63/63 теста.

## v2026.07.24.2-msk-headerless-library-import — 24.07.2026 13:55

- В окне "Расчет материалов" кнопка `Сделать заявку Bitrix 24` переименована в `Сформировать заявку`.
- Импорт библиотеки теперь распознает файл `Данные из МСК.xlsx` без строки заголовков: колонки читаются как IPS, обозначение, наименование детали, вид/размер/материал заготовки, норма, единица, УТ-код и наименование заготовки.
- Для такого МСК-формата добавлен отдельный профиль `MskHeaderlessBlankImportProfile`, поэтому импорт обновляет детали, заготовки, УТ-коды, нормы расхода и единицы измерения в библиотеке.
- Добавлена регрессионная проверка импорта беззаголовочного МСК-файла; `dotnet test BlankDemandPlanner.sln` прошел, 62/62 теста.

## v2026.07.24.1-bitrix-request-onetime-blank — 24.07.2026 13:25

- Во вкладке "Потребность" из детализации убрана колонка `Ост. до`, панель детализации расширена, а рядом с `всего деталей` добавлен показатель `дефицит` как потребность минус количество в производстве.
- В окне "Расчет материалов" кнопка экспорта переименована в `Сделать заявку Bitrix 24`, а колонка `Дата потребности заготовки` расширена, чтобы заголовок помещался целиком.
- Поиск заготовки в НСИ для расчета стал нечувствителен к регистру и ищет по наименованию, материалу, коду УТ и алиасам.
- Контекстное меню строки расчета теперь позволяет добавить заготовку для выбранной строки как разовое назначение текущего расчета; библиотека и НСИ при этом не изменяются.
- Excel-заявка формируется файлом `Заявка на закуп заготовок ЦМО от <дата> <номер>.xlsx`, содержит только листы `Заявка` и `По группе`; из заявки убрана колонка `В производстве`.
- Лист `По группе` суммирует одинаковые УТ-коды, а для строк без УТ использует IPS как код группировки; добавлены регрессионные проверки разового назначения и группировки `0,85 + 0,46 = 1,31`.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошел, 61/61 тест.

## v2026.07.23.5-stock-warehouse-groups — 23.07.2026 18:20

- Расширены правила остатков 1С: НЗП/остаток на ЦМО теперь суммируется по `44 секция НЗП (незавершенное производство)` и `Детали МУ в обработке на стороне`.
- Колонка НСИ `Остаток на складе` переименована в `Склад`; складской остаток теперь суммируется по `1 секция (ПЗМЦ) только для производственных целей`, `Покраска ТМЦ`, `ТМЦ по проекту ФРП`, `ТМЦ для проверки ОТК`, `ТМЦ для временного хранения ЦСТС`, `ТМЦ для временного хранения ЦМО`.
- Синхронизация 1С теперь передает в Python-выгрузку несколько складов для одной роли и сохраняет их в один снимок остатков.
- Обновлены статусы обновления 1С: вместо `склад 1` и `НЗП 44` показываются группы `Склад` и `НЗП/ЦМО`.
- Добавлены регрессионные проверки расчета и НСИ по новым группам складов; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.4-msk-detail-panel-layout — 23.07.2026 17:45

- Во вкладке "МСК" окно "Информация по детали" перенесено вправо и занимает половину рабочей области вкладки; добавлена прокрутка, чтобы помещалась вся детальная информация.
- Информация по детали теперь обновляется без открытия Excel: автоматически после поиска с одним результатом или точным IPS, при выборе строки и при наведении на строку таблицы.
- Добавлены обработчики наведения и выбора строк МСК, чтобы правая панель всегда показывала текущий объект.
- Добавлена регрессионная проверка автопоказа информации после поиска; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.3-msk-detail-blank-selection-fix — 23.07.2026 17:20

- Во вкладке "МСК" кнопка обновления переименована в `Обновить данные МСК`.
- Исправлено открытие файла МСК после поиска: если строка не выделилась явно, команда берет единственную найденную строку или точное совпадение по IPS.
- Нижняя панель "Информация по детали" показывает расширенные атрибуты: IPS, обозначение, наименование, вид заготовки, заготовку, материал, код УТ, норму расхода, единицу и срок.
- Таблица МСК дополнена колонками по заготовке, материалу, коду УТ, норме и сроку; атрибуты берутся из активной связи библиотеки, а при ее отсутствии - из CSV-анализа МСК.
- В "Подборе заготовок" выбор детали из выпадающего списка фиксируется в поле и больше не сбрасывается после обновления подсказок.
- При вводе первых символов в поиске детали без заготовки список вариантов раскрывается автоматически.
- Поиск заготовок по материалу стал искать по материалу, каноническому наименованию и алиасам НСИ; запрос вроде `Сталь` теперь находит подходящие позиции.
- Добавлены регрессионные тесты на фиксацию выбранной детали, поиск по материалу и расширенную карточку МСК; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.2-msk-window-library-delete — 23.07.2026 16:30

- Исправлено удаление строк "Библиотеки" для деталей без заготовки, которые уже пришли из потребности: карточка архивируется и пропадает из списка, строки потребности отвязываются, действие можно отменить.
- Добавлена рабочая вкладка "МСК" вместо заглушки.
- Вкладка "МСК" читает Excel-файлы из `X:\19_МЕХ УЧАСТОК\База МСК\СПИСОК МСК`, извлекает IPS, обозначение, наименование и путь к исходному файлу.
- Реестр МСК сводится с библиотекой по IPS: в таблице видно, есть ли деталь в библиотеке и есть ли для нее файл МСК.
- Добавлен поиск МСК по IPS, обозначению, наименованию и имени файла; при выборе строки в нижнем окне показывается информация по детали.
- Добавлена кнопка `Открыть Excel МСК`, которая открывает исходный файл МСК через Excel/системное приложение.
- Добавлены регрессионные тесты на удаление детали без заготовки и сведение МСК с библиотекой; `dotnet test BlankDemandPlanner.sln` прошел, 57/57 тестов.

## analysis-2026.07.23-msk-library-extract — 23.07.2026

- Пересобран анализ Excel-базы МСК `X:\19_МЕХ УЧАСТОК\База МСК` для будущего переноса библиотеки в ПО.
- Итоговый файл `reports/msk_excel_analysis_20260723/msk_library_extract.xlsx` содержит основной лист `Библиотека МСК`, отдельный лист `Варианты УТ` и лист `Дубли IPS`.
- Колонка `Источник` в Excel сделана гиперссылкой на исходный файл МСК; служебные колонки `Раздел`, `Форма` и фактические поля убраны из основного листа.
- При дублировании IPS приоритет отдается актуальной папке `СПИСОК МСК`; отклоненные варианты вынесены на отдельный лист.
- Заготовка и размер разделяются, нормы рассчитываются из размеров: длинномерные заготовки по `L` переводятся в `пог. м`, листы/плиты/поковки/литье принимаются как `1 шт`.
- Уточнена раскладка для будущей библиотеки: ГОСТ материала и ГОСТ сортамента вынесены в отдельные колонки, из наименования заготовки убираются класс проката и ГОСТ.
- Вариант УТ-кода и номенклатура НСИ перенесены в основной лист `Библиотека МСК`; отдельный лист `Варианты УТ` больше не создается.
- Подбор УТ-кода теперь требует совпадения вида, материала и основного размера профиля; контрольный пример `IPS 1320756 / D80 / Сталь 40Х` выбирает `УТ000022657`, а не позицию `D330`.
- Код приложения не менялся, сборка не публиковалась.

## v2026.07.23.1-stock-code-normalization-toolbar — 23.07.2026 10:51

- Исправлено сопоставление IPS/УТ с остатками 1С: цифровые коды сравниваются как 11-значные коды 1С с ведущими нулями, поэтому `1802132` корректно совпадает с `00001802132`, а коды вида `УТ000004539` не смешиваются с IPS.
- Колонка "В производстве" во вкладке "Потребность", расчет материалов, экспорт и НСИ используют единое правило сопоставления кодов остатков 1С.
- При синхронизации 1С новый снимок остатков привязывает строки к существующим алиасам НСИ по нормализованному коду, даже если в библиотеке код был занесен без ведущих нулей.
- Из верхней панели убрана кнопка `Импорт остатков Excel`; рабочее обновление остатков выполняется кнопкой `Обновить данные 1С`.
- Верхняя панель переведена на переносимую компоновку, чтобы кнопки, статус 1С, поиск и отмена помещались в рабочую область при изменении размера окна.
- Добавлен регрессионный тест на пример `1802132 -> 00001802132`; `dotnet test BlankDemandPlanner.sln` прошел, 55/55 тестов.

## v2026.07.22.2-onec-stock-sync-sections — 22.07.2026 12:12

- Добавлена read-only синхронизация остатков из 1С через регистр `ТоварыНаСкладах` с записью результата в обычный `StockSnapshot` приложения.
- Закреплены секции: остаток ЦМО/НЗП берется только из `44 секция НЗП (незавершенное производство)`, остаток на складе для производственных целей - только из `1 секция (ПЗМЦ) только для производственных целей`.
- При запуске ПО синхронизация остатков 1С стартует в фоне; в верхней панели добавлена кнопка `Обновить данные 1С` и строка с датой/итогом последнего обновления.
- Excel-импорт остатков сохранен как резервная кнопка `Импорт остатков Excel`.
- Расчет материалов и отображение НСИ используют те же секционные правила: заготовки вычитаются только по складу 1, НЗП деталей - только по секции 44.
- Live-проверка доступа к 1С прошла: подключение к `pz-sql1.pzmc.org:1541`, выгружено 7408 строк склада 1 и 345 строк секции 44.
- Добавлен регрессионный тест секционных правил расчета; `dotnet test BlankDemandPlanner.sln` прошел, 54/54 теста.

## v2026.07.22.1-library-click-import-new-nomenclature — 22.07.2026 11:26

- Исправлен сбой в "Библиотеке" при наведении и клике по строке: таблица остается в режиме выбора ячеек, но больше не пытается программно выбирать всю строку через `SelectedItem`.
- В "Расчете материалов" строки без определенной заготовки теперь показывают в колонке "Номенклатура" значение "Новая номенклатура".
- Импорт экспортированного файла `Библиотека.xlsx` теперь понимает колонки общего файла, обновляет наименование/обозначение детали, количество и срок заготовки после ручного редактирования Excel.
- Добавлены регрессионные тесты на расчет без заготовки и цикл `Выгрузить в Excel -> отредактировать -> импортировать`; `dotnet test BlankDemandPlanner.sln` прошел, 53/53 теста.

## v2026.07.21.10-library-hover-context-crash-fix — 21.07.2026 16:00

- Исправлен сбой при открытии "Библиотеки" и наведении/появлении всплывающего списка: команды контекстного меню теперь устойчивы к служебным WPF-параметрам и не падают на `MS.Internal.NamedObject`.
- Команда "Редактировать" в библиотеке безопасно игнорирует параметры, которые не являются строкой библиотеки, и использует выбранную строку только когда она действительно доступна.
- Добавлен регрессионный тест на служебные параметры WPF для команд "Редактировать" и "Удалить"; `dotnet test BlankDemandPlanner.sln` прошел, 51/51 тест.

## v2026.07.21.9-library-cell-nsi-count-calc-button-delete-fix — 21.07.2026 15:34

- В "Библиотеке" таблица переведена в режим выделения ячеек `Cell`, чтобы при клике выделялась конкретная ячейка, а не вся строка.
- В НСИ добавлена инфострока с количеством показанных строк по аналогии с "Библиотекой".
- Во вкладку "Расчет материалов" возвращена кнопка расчета с новым названием "Произвести расчет".
- Из верхней панели рядом с "Импорт остатков по 1С" убрана кнопка "Расчет материалов".
- Удаление строки библиотеки сделано устойчивым к служебным WPF-параметрам контекстного меню, чтобы ошибка `MS.Internal.NamedObject` не приводила к падению.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошел, 50/50 тестов.

## v2026.07.21.8-library-demand-detail-filter-edit — 21.07.2026 14:35

- В окне "Расчет материалов" убрана верхняя кнопка "Расчет материалов"; запуск расчета остается через общую кнопку приложения.
- При добавлении новых IPS из потребности в библиотеку теперь учитываются только детали: строки операций, сборок, узлов, комплектов и монтажных работ не создают карточки деталей.
- Новые детали из потребности разбираются на "Обозначение" и "Наименование", если строка начинается с обозначения детали.
- В "Библиотеке" исправлено сохранение "Обозначения" и "Наименования" для карточек деталей без выбранной заготовки.
- В "Библиотеке" добавлен фильтр "Связь": все, без заготовки, с заготовкой.
- Добавлены регрессионные тесты для фильтрации деталей из потребности и сохранения наименования/обозначения без заготовки; `dotnet test BlankDemandPlanner.sln` прошел, 50/50 тестов.

## v2026.07.21.7-stable-user-data-demand-delete-library-ips — 21.07.2026 14:24

- База данных приложения перенесена в устойчивое хранилище `Данные для работы\BlankDemandPlanner.db`; при первом запуске новая база копируется из старой `%LOCALAPPDATA%\BlankDemandPlanner\BlankDemandPlanner.db`, чтобы ручные правки библиотеки и НСИ сохранялись между обновлениями ПО.
- В "Потребности" добавлена кнопка "Удалить выделенные"; можно удалить выделенные строки, действие попадает в общий механизм отмены.
- "Библиотека" перед загрузкой сверяет IPS из потребности и создает отсутствующие карточки деталей без заготовки, чтобы новые IPS появлялись в библиотеке.
- Добавлены регрессионные тесты для удаления строк потребности с отменой и для автоматического добавления отсутствующих IPS из потребности в библиотеку; `dotnet test BlankDemandPlanner.sln` прошел, 49/49 тестов.

## v2026.07.21.6-dashboard-nsi-usage-library-delete-demand-parts — 21.07.2026 14:08

- В главном виде убраны карточки "К закупке" и "Вне И-012"; оставшиеся карточки сделаны одинакового размера.
- Главные показатели оставлены привязанными к базе и обновляются при открытии главной страницы после импортов, расчетов и изменений справочников.
- Исправлено обновление окна "Применяемость" в НСИ при ячеечном выделении: клик по строке/ячейке снова показывает связанные детали.
- Исправлена ошибка удаления в "Библиотеке" из лога `MS.Internal.NamedObject`: контекстное меню удаляет одну строку, кнопка "Удалить выделенные" работает по выделенным ячейкам/строкам.
- Для строк библиотеки без заготовки добавлено полноценное удаление карточки детали с отвязкой строк потребности по `PartId`.
- В редактор НСИ для поля "Марка материала" добавлены выпадающие подсказки по уже имеющимся материалам из НСИ.
- При обновлении существующей партии потребности импорт теперь создает недостающую карточку детали по IPS и привязывает ее к строке потребности, чтобы деталь появилась в библиотеке без заготовки.

## v2026.07.21.5-selection-detail-library-delete-history-time — 21.07.2026 13:48

- Исправлено окно детализации "Потребности" после перехода на ячеечное выделение: клик по ячейке снова обновляет детализацию выбранного IPS.
- Исправлено удаление в "Библиотеке" для ячеечного выделения: команда "Удалить выделенные" собирает строки из выделенных ячеек и удаляет/архивирует выбранные связи.
- Автозаполнение редактора "Библиотеки" закреплено за выбором ячейки/строки и действием "Редактировать".
- Поведение редактора НСИ откатано: простое выделение строки обновляет применяемость, а редактор заполняется только через контекстное действие "Редактировать".
- Во вкладку "История" добавлена колонка "Дата обновления" после версии; новые записи читают дату и время из заголовка версии в `VERSION.md`.

## v2026.07.21.4-cell-selection-library-no-blank-counts

- Во всех основных таблицах выделение приведено к режиму ячеек: клик по строке выбирает конкретную ячейку, как в "Расчете материалов".
- Вкладка "Подбор заготовок" перенесена после вкладки "НСИ".
- В детализации "Потребности" в заголовок после IPS добавлено общее количество деталей в потребности по выбранному IPS.
- "Библиотека" теперь показывает детали без активной заготовки, добавлена верхняя инфострока с количеством показанных деталей и количеством деталей без заготовки по текущим фильтрам.
- Редактор "Библиотеки" заполняется при выборе/редактировании строки аналогично редактору НСИ.
- В "Расчет материалов" перед колонкой "Номенклатура" добавлена колонка "Вид заготовки".
- Список деталей без заготовки в "Подборе заготовок" синхронизирован с теми же деталями, которые теперь видны в "Библиотеке".
- Добавлены регрессионные тесты для показа деталей без заготовки в библиотеке и выбора их в подборе; `dotnet test BlankDemandPlanner.sln` прошел, 47/47 тестов.

## v2026.07.21.3-blank-selection-library-tool

- Вкладка "Подбор заготовок" дополнена выбором деталей без активной заготовки: можно набрать IPS вручную или выбрать деталь из выпадающего списка.
- По выбранной детали, найденной заготовке и норме расхода теперь можно сразу создать связь в "Библиотеке" кнопкой "Добавить в библиотеку"; старая активная связь по детали при необходимости архивируется.
- При выборе заготовки единица расхода по умолчанию подтягивается из НСИ, норму можно указать вручную в `шт` или `пог. м`.
- После импорта потребности из ПП приложение считает новые IPS, добавляет их как детали без заготовки и сообщает пользователю количество добавленных новых деталей.
- Добавлен регрессионный тест на сохранение выбранной детали и заготовки из вкладки "Подбор заготовок"; `dotnet test BlankDemandPlanner.sln` прошел, 46/46 тестов.

## v2026.07.21.2-demand-refresh-editors-blank-selection

- Исправлено обновление окна "Потребность" после последовательной загрузки файлов ПП: повторный импорт уже известного файла теперь поднимает его партию в последние, а экран показывает именно последнюю импортированную потребность.
- Добавлен регрессионный тест с несколькими файлами `*Потребность*.xlsx` с рабочего стола: последовательный импорт и повторный импорт первого файла обновляют видимый список.
- В "Библиотеке" и "НСИ" контекстное меню строки приведено к двум действиям: "Редактировать" и "Удалить"; редактирование заполняет верхний редактор выбранной строки.
- В редакторы "Библиотеки" и "НСИ" добавлена явная кнопка "Очистить".
- Подтверждена и исправлена проблема накопления разных активных заготовок для одного IPS: при сохранении или импорте новой основной заготовки старые активные связи IPS архивируются.
- Добавлена новая вкладка "Подбор заготовок": по виду, материалу и размерам детали подбирает заготовки из НСИ, которые больше или равны детали, и сортирует их от меньшего припуска к большему.
- Добавлены регрессионные тесты для обновления потребности, замены активной заготовки IPS и подбора круглой заготовки; `dotnet test BlankDemandPlanner.sln` прошел, 45/45 тестов.

## v2026.07.21.1-ips-viewer-research

- Выполнена разведка установленного ClickOnce-приложения `IPS Viewer` для будущей привязки деталей к данным IPS и PDF-чертежам.
- Найдены локальный кэш приложения, источник публикации, классы `Sql` / `ObjectIPS` / `LinkIPS` / `FileIPS` и основные таблицы IPS: `IMS_OBJECTS`, `IMS_OBJECT_ATTRS`, `IMS_RELATIONS`, `IMS_STORAGE`.
- Зафиксировано, что PDF-чертежи читаются из `IMS_STORAGE.F_FILEBODY` по `F_FILE_ID`, а список файлов объекта фильтруется через `F_ATTRIBUTE_ID=1002`.
- Рабочая строка подключения к базе IPS пока не найдена: сетевой каталог публикации недоступен на чтение, тест SQL к `pz-sql1` завершился таймаутом.
- Добавлен документ `docs/IPS_VIEWER_INTEGRATION_NOTES.md` с найденными запросами, ограничениями и рекомендуемым read-only планом внедрения.
- Код приложения не менялся, новая сборка не публиковалась.

## v2026.07.20.16-onec-stock-fast-under-30

- Быстрый запрос остатков 1С оптимизирован под оперативные ответы: по умолчанию читается только основной регистр `ТоварыНаСкладах`.
- Сверка с `ПартииТоваровНаСкладах` и `ТоварыОрганизаций` перенесена на явный флаг `--control`.
- Добавлен пакетный режим до 30 строк в одном COM-подключении: `lookup_1c_stock.cmd --items-file <файл> --warehouse <секция>`.
- Для пользовательского ввода кодов УТ добавлена нормализация, например `11445` дополнительно проверяется как `УТ000011445`.
- Так как холодный COM-старт занимает около 51 секунды, подготовлен прогретый read-only helper-сервер `stock_lookup_server.py` с запуском через `start_stock_lookup_server.cmd` и запросами через `query_stock_server.cmd`.
- Проверка прогретого helper-сервера: запрос `11445` по секции `44` отработал за 0,7 секунды.
- Проверка: Python unit-тесты интеграции прошли 4/4; компиляция новых Python-скриптов прошла; `dotnet test BlankDemandPlanner.sln` прошел 39/39.
- Быстрый путь закреплен в `AGENTS.md`, `PROJECT_CONTEXT.md`, `ONEC_STOCK_FAST_PATH.md`, README и `SESSION_HANDOFF.md`.

## v2026.07.20.15-onec-diagnostics-path-move

- Обновлен закрепленный путь диагностического контура 1С: теперь используется `C:\Users\dpd\Documents\Codex\1C_Diagnostics`.
- Быстрая команда `onec_integration\lookup_1c_stock.cmd` перенастроена на новый путь Python venv и `.env`.
- Обновлены `AGENTS.md`, `PROJECT_CONTEXT.md`, `SESSION_HANDOFF.md`, README и инструкции `ONEC_STOCK_FAST_PATH.md` / `STOCK_SYNC_IMPLEMENTATION_PLAN.md`, чтобы будущие запросы остатков сразу использовали новое расположение.
- Проверка: быстрый live-запрос остатков вернул `2`; Python-тесты интеграции прошли 4/4; `dotnet test BlankDemandPlanner.sln` прошел 39/39.

## v2026.07.20.15-demand-nsi-dashboard-calculation-fix

- Во вкладке "Потребность" детализация больше не меняется от простого перемещения мыши: панель обновляется по выбранной строке, аналогично применяемости в НСИ.
- После загрузки потребности из ПП экран принудительно очищает трекинг БД и обновляет вкладку "Потребность" и главную страницу.
- Колонка "В производстве" в потребности показывает фактический остаток НЗП перед текущей строкой, то есть уже за вычетом строк выше по тому же IPS.
- В окне "Расчет материалов" скрываются строки с нулевым количеством к заказу, а колонка "В производстве" убрана.
- В НСИ после "Остаток на ЦМО" добавлена колонка "Остаток на складе".
- На главной странице карточки показателей стали активными: нажатие открывает соответствующую вкладку.
- Добавлены регрессионные тесты для НЗП в потребности, скрытия нулевых строк расчета и раздельных остатков НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 41/41 тест.

## v2026.07.20.14-demand-details-panel

- Во вкладке "Потребность" окно детализации перестроено по образцу панели применяемости НСИ: основная таблица и детализация расположены рядом.
- Добавлена кнопка "Скрыть детализацию" / "Отобразить детализацию".
- Наведение на строку потребности продолжает обновлять детализацию по IPS.
- Добавлен регрессионный тест для переключения панели детализации; `dotnet test BlankDemandPlanner.sln` прошел, 39/39 тестов.

## v2026.07.20.13-nsi-size-demand-details

- В НСИ исправлено сохранение правки при размере, который уже отображается в формате `D... W... H... T... S... L...`; редактор теперь понимает такие размеры и не блокирует изменение из-за рассогласования формата.
- Во вкладку "Потребность" добавлено окно "Детализация IPS" в правой верхней части.
- При наведении на строку потребности окно детализации показывает все строки по тому же IPS: дату потребности, количество деталей, остаток "В производстве" до строки, списание по строке, остаток после строки, проект и № станка.
- Добавлены регрессионные тесты для сохранения НСИ с форматом размера `W60 H174 L970` и детализации повторяющегося IPS; `dotnet test BlankDemandPlanner.sln` прошел, 38/38 тестов.

## v2026.07.20.13-onec-stock-fast-path

- Закреплен быстрый read-only путь получения текущих остатков из 1С УТ через COM без Excel: `onec_integration\lookup_1c_stock.cmd`.
- Подтверждены реальные регистры для остатков: основной `ТоварыНаСкладах`, контрольные `ПартииТоваровНаСкладах` и `ТоварыОрганизаций`, ресурс `Количество`.
- Для проблем с DNS/FQDN добавлен рабочий fallback подключения `pz-sql1:1541`; учетные данные остаются во внешнем `C:\Users\dpd\Documents\Codex\1C_Diagnostics\.env`.
- Добавлены инструкции `ONEC_STOCK_FAST_PATH.md` и `STOCK_SYNC_IMPLEMENTATION_PLAN.md`, чтобы в будущем внедрить прямую синхронизацию остатков в ПО вместо Excel-импорта.
- `config.example.json` заполнен известными метаданными остатков УТ 10.3, чтобы не начинать последующие запросы с повторного обследования.
- Проверочный live-запрос для `01-P002-03.002-T63.150.SS` и секции `44` вернул остаток `2` в основном и контрольных регистрах.
- Проверка: Python-тесты интеграции прошли, 4/4; `dotnet test BlankDemandPlanner.sln` прошел, 38/38.

## v2026.07.20.12-demand-wip-lead-time-fix

- Во вкладке "Потребность" исправлена колонка "В производстве": остатки 1С теперь сопоставляются с IPS без учета ведущих нулей, например `00001799544` засчитывается для IPS `1799544`.
- Статус потребности после "уникальных IPS" показывает суммарное количество деталей по отображенным строкам.
- В НСИ объединены кнопки удаления: оставлена одна кнопка "Удалить", работающая для одной или нескольких выделенных строк.
- В библиотеку добавлено поле и колонка "Срок заготовки, дней" с базовым значением 30; значение можно менять вручную для связи детали с заготовкой.
- В расчете материалов "Дата потребности заготовки" теперь считается как дата потребности детали минус срок из библиотеки; базово 30 дней.
- Excel-выгрузка расчета использует ту же дату потребности заготовки и получила чистые русские заголовки листа "Заявка".
- В выгрузку библиотеки добавлен срок заготовки в днях.
- Добавлена миграция `20260720190000_AddBlankLeadTimeDays`; `dotnet test BlankDemandPlanner.sln` прошел, 36/36 тестов.

## v2026.07.20.11-onec-integration-foundation

- Добавлен отдельный проект `onec_integration` для безопасной интеграции с 1С:УТ 10.3: Python COM-клиент, CLI, конфиг, журналирование, обработка ошибок, тесты и инструкции.
- Добавлены шаблоны 1С-модулей для обследования метаданных и будущего HTTP-сервиса расширения без создания документов на диагностическом шаге.
- Запись документов защищена флагами `DRY_RUN`, `ALLOW_DOCUMENT_WRITE` и `ALLOW_DOCUMENT_POSTING`; по умолчанию доступны только чтение, валидация и dry-run.
- Добавлены `.env.example` и `.gitignore`, чтобы пароль 1С, логи и временные файлы не попадали в исходники.
- Выполнена локальная проверка окружения: толстый клиент 1С 8.3.27.2214 найден, но DNS-имя `pz-sql1.pzmc.org` на этой машине не разрешилось, поэтому фактическое обследование базы 1С не выполнено.
- Проверка: unit-тесты Python `python -m unittest discover onec_integration/tests` прошли, 3/3; dry-run команды комплектования и перемещения возвращают успешный предварительный результат.

## v2026.07.20.10-demand-library-nsi-edit-fix

- Повторно исправлено отображение номера станка вида `�25633`: очистка больше не портит уже исправленную букву `Р` и заменяет ошибочный символ перед цифрами после восстановления кодировки.
- Во вкладку "Потребность" добавлена колонка "В производстве"; значение считается по последнему снимку остатков 1С, где код совпадает с IPS и единица равна `шт`.
- Для нескольких строк одного IPS остаток "В производстве" распределяется сверху вниз по видимому порядку строк: верхняя строка забирает свою часть первой, следующая получает остаток.
- В библиотеке исправлено ручное добавление детали с заготовкой: выбор из выпадающего списка удерживается, автоматически заполняет код УТ, а при сохранении заготовка дополнительно находится по тексту `наименование | код`.
- В НСИ исправлено ручное редактирование заготовки: можно менять код УТ / IPS, наименование, вид, единицу, материал и размер; дубль кода ловится до сохранения.
- Убраны старые поврежденные статусные сообщения из сохранения правки НСИ.
- Добавлены регрессионные тесты для номера станка `�25633`, распределения "В производстве" по строкам IPS, сохранения библиотеки по `наименование | код` и ручной правки НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 33/33 тестов.

## v2026.07.20.9-forging-library-search-fix

- В НСИ убрана лишняя кнопка "Отобразить применяемость"; остается рабочая кнопка скрытия/отображения панели, а применяемость загружается по выбранной строке.
- В справочник видов заготовок добавлен новый тип "Поковка"; нормализация импорта распознает слово "поковка".
- В библиотеке исправлен выбор покупных заготовок: ручной выбор вида "Покупная" больше не конфликтует с автоматическим распознаванием размеров `D...`.
- Поиск заготовки в библиотеке теперь понимает формат `наименование | код`, например `Заготовка оправки ВТ40-D70-L175 | 1802132`.
- Для покупных заготовок список подсказок допускает ранее импортированные позиции с типом "Прочее" или "Не распознано", чтобы их можно было выбрать и связать с деталью.
- Добавлены регрессионные тесты для распознавания "Поковка" и поиска покупной заготовки по примеру `1802132`; `dotnet test BlankDemandPlanner.sln` прошел, 29/29 тестов.

## v2026.07.20.8-machine-nsi-stock-request-fix

- В номере станка добавлена очистка ошибочного символа `�` перед цифрами: значения вида `�25604` отображаются как `Р25604`.
- В НСИ добавлена кнопка `+` для ручного занесения заготовки в базу; форма позволяет заполнить код УТ / IPS, наименование, вид, единицу, марку, размер и ГОСТы.
- Экран "Расчет материалов" теперь показывает в заявке только количество к закупке после вычета складских остатков; полностью обеспеченные складом строки не попадают в заявку.
- Выгрузка "Расчета материалов" в Excel использует ту же логику: если склад закрывает потребность, строка не выгружается как дефицит.
- Excel-экспорт расчета и библиотеки снова приведен к нормальным русским именам файлов, листов и заголовков.
- Применяемость НСИ устойчивее сопоставляет очищенные старые строки и новые данные.
- Добавлен регрессионный тест на потребность `0,133 пог. м`, полностью покрытую складом; `dotnet test BlankDemandPlanner.sln` прошел, 27/27 тестов.

## v2026.07.20.7-material-export-library-nsi-fix

- В "Расчете материалов" восстановлена выгрузка Excel: файл сохраняется как `Расчет_материалов_<номер>.xlsx`, а если файл занят Excel, создается резервная копия с датой и временем.
- Экспорт расчета получил нормальные русские имена листов и заголовков без mojibake.
- В библиотеку добавлена кнопка `+` для ручного добавления новой строки через редактор.
- В НСИ добавлена отдельная команда "В архив" для нескольких выделенных строк; одиночные команды удаления и архива также перенаправлены на общий рабочий механизм выделения.
- Окно применяемости НСИ восстановлено: кнопка явно загружает применяемость выбранной строки, а сопоставление дополнительно учитывает алиасы и наименования заготовок.
- В списке "Вид заготовки" убран дубль "Не распознано"; старый технический тип `CustomBlank` отображается как "Прочее".
- Дополнительно очищены видимые сообщения импорта/редактирования НСИ и библиотеки от старых иероглифов.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошла, 26/26 тестов; были предупреждения NU1900 из-за недоступного `https://api.nuget.org/v3/index.json`.

## v2026.07.20.6-dashboard-popup-export-selection-fix

- На главной странице исправлены подписи разработчика и версии: старые иероглифы заменены на нормальный русский текст и текущую версию.
- В статусах библиотеки и НСИ добавлена автоматическая очистка старого mojibake-текста перед отображением.
- Заголовки всплывающих окон импорта библиотеки и НСИ восстановлены нормальным русским текстом.
- В библиотеке убрана кнопка удаления из каждой строки; удаление доступно через правую кнопку мыши по строке и через кнопку "Удалить выделенные".
- Выгрузка библиотеки больше не падает, если `Библиотека.xlsx` открыт в Excel: приложение сохраняет резервный файл `Библиотека_yyyyMMdd_HHmmss.xlsx`.
- В НСИ колонка "Остаток на ЦМО" показывает `0`, если остатка по позиции нет в последнем снимке.
- Добавлены регрессионные тесты для занятого файла выгрузки и нулевого остатка НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 26/26 тестов.

## v2026.07.20.5-library-export-nsi-stock-fix

- В поисковом фильтре библиотеки по единице измерения убрана единица `кг`; остались `Все ед.`, `шт`, `пог. м`.
- Удаление строк библиотеки усилено: строка архивируется без физического удаления детали, кнопка удаления добавлена прямо в строку, а массовое удаление понимает выделенные ячейки и строки.
- Выгрузка библиотеки теперь создает в выбранной папке ожидаемый файл `Библиотека.xlsx` и заменяет предыдущий файл с таким именем.
- Сообщения выгрузки библиотеки восстановлены нормальным русским текстом.
- В НСИ колонка "Остаток факт" переименована в "Остаток на ЦМО"; колонка "Ед. остатка" убрана.
- Добавлены регрессионные тесты для фильтра единиц библиотеки, удаления строки библиотеки и имени файла выгрузки; `dotnet test BlankDemandPlanner.sln` прошел, 25/25 тестов.

## v2026.07.20.4-demand-undo-copy-calculation-fix

- Вкладка "Потребность": убрана колонка PS, добавлена колонка "IPS детали", "Серийный №" заменен на "№ станка".
- В статусе потребности рядом с количеством строк добавлен счетчик уникальных IPS.
- В потребности добавлены ручное добавление строки кнопкой `+`, сохранение ручных строк и ограничитель по дням от текущей даты потребности.
- В библиотеке из редактора единиц расхода убрана единица `кг`; удаленные/архивированные связи больше не остаются пустыми строками в списке.
- Исправлена причина повторного расчета одинаковой строки: одинаковые активные связи одной детали с одной заготовкой и единицей теперь учитываются один раз.
- В расчет материалов и основные окна импорта возвращены нормальные русские статусы без mojibake.
- Копирование из таблиц теперь выполняется без заголовка колонки.
- Таблицы переключены на выбор ячеек/заголовков строк вместо принудительного выделения всей строки.
- Добавлена кнопка "Отменить" в правом верхнем углу; хранится до 10 последних действий для ручной потребности, удаления библиотеки и ручного назначения заготовки.
- Добавлен регрессионный тест на дубли активных привязок; `dotnet test BlankDemandPlanner.sln` прошел, 22/22 тестов.
- Опубликована сборка в `publish/versions/v2026.07.20.4-demand-undo-copy-calculation-fix`.
## v2026.07.20.3-ui-text-history-settings

- Восстановлены читаемые русские значения единиц измерения и видов заготовок в библиотеке, НСИ, расчете материалов и выборе заготовок.
- Добавлена очистка старых mojibake-строк при отображении обозначений, наименований, складов и подсказок заготовок без изменения данных в базе.
- В НСИ добавлена команда "Удалить выделенные" для удаления нескольких выбранных строк; позиции с историей остатков переводятся в архив.
- В НСИ фактический нулевой остаток теперь отображается как `0`, если строка остатков реально есть.
- Вкладка "История" теперь показывает версии обновлений и краткое описание работ из `VERSION.md`.
- Вкладка "Настройки" получила изменение шрифта, размера шрифта, положения верхних кнопок, выбор иконки окна и пользовательского изображения.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошла, 21/21 тестов; предупреждения NU1900 связаны с недоступностью `https://api.nuget.org/v3/index.json`.
- Опубликована сборка в `publish/versions/v2026.07.20.3-ui-text-history-settings`.
## v2026.07.20.2-update-version-workflow

- Закреплено правило: все работы по опубликованным обновлениям, сборкам и версиям ведутся в `C:\Users\dpd\Documents\Заказ заготовок для ЦМО\publish\versions`.
- Для каждой опубликованной версии должна создаваться отдельная папка `publish/versions/<version>`, совпадающая с заголовком версии в этом журнале.
- После завершения обновления ПО "Заказ заготовок ЦМО" должно открываться для пользовательской проверки.
- Обновлены проектные инструкции `AGENTS.md`, `PROJECT_CONTEXT.md` и `SESSION_HANDOFF.md`.
- Опубликована сборка в `publish/versions/v2026.07.20.2-update-version-workflow`.

## v2026.07.20.1-project-memory-recovery

- Восстановлена проектная память по работе за 2026-07-13 - 2026-07-17 на основании исходных файлов, текущих исходников, тестов, `VERSION.md`, `publish/versions` и временных логов сборки.
- Добавлен файл `RECOVERY_2026-07-13_17.md` с хронологией, источниками восстановления, ограничениями и защитным алгоритмом.
- Зафиксировано, что `.git` в проекте пустой, а команда `git` в текущем PowerShell-окружении недоступна; историю нельзя считать восстановимой через reflog/commits.
- В проектные правила добавлен обязательный протокол завершения треда: обновлять handoff, журнал версий, проверку и публикационные артефакты.

## v2026.07.17.3-ui-library-nsi-fix

- Во вкладке "Потребность" восстановлены читаемые русские статусы в верхней части экрана.
- В левом меню "Расчет материалов" перенесен сразу после "Потребность"; отдельная вкладка отчетов не добавляется и не используется.
- В библиотеке удаление работает для одной и нескольких выделенных строк: добавлена команда "Удалить выделенные", контекстное меню удаляет весь текущий выбор.
- Для НСИ добавлено аварийное восстановление: если активный справочник пуст, приложение возвращает архивные позиции, а при полном отсутствии НСИ поднимает временные строки из последнего снимка остатков.
- Исправлены отображаемые русские значения видов заготовок и единиц измерения в библиотеке/НСИ.

## v2026.07.17.2-wip-in-material-calculation

- В расчете материалов добавлена колонка "В производстве".
- Строки остатков 1С, где код совпадает с IPS детали из потребности, учитываются как НЗП.
- НЗП вычитается из количества деталей перед расчетом потребности в заготовках; при нескольких строках одного IPS НЗП распределяется последовательно и не вычитается повторно.
- Excel-лист "Заявка" дополнен колонкой "В производстве" и выгружает количество материала уже за вычетом НЗП.
- Добавлен регрессионный тест: потребность 10 деталей, НЗП 4 детали, остаток заготовки 2 шт. дает к закупке 4 шт.

## v2026.07.17.1-library-nsi-material-calculation

- В библиотеке добавлена команда удаления данных: строку можно удалить через контекстное меню правой кнопкой мыши.
- Вкладка "Остатки" убрана из левого меню; фактический остаток последнего снимка склада теперь отображается в НСИ после колонки "ГОСТ сортамента".
- "Расчет" переименован в "Расчет материалов"; таблица расчета приведена к форме заявки для снабжения: №, ПС, № станка, IPS, наименование, количество деталей, код УТ, номенклатура, ед. измерения, количество и дата потребности.
- Для строк расчета без подобранной заготовки добавлена ручная привязка заготовки из НСИ с количеством на деталь и единицей измерения.
- Экспорт расчета в Excel дополнен первым листом "Заявка" в формате для формирования закупочной потребности.

## v2026.07.16.16-demand-refresh

- Исправлено отображение ранее загруженных строк потребности, где `Наименование операции` ошибочно попадало в колонку `Наименование`, а `Проект`, `Серийный №` и `Ед. изм.` оставались пустыми.
- Повторная загрузка того же файла потребности больше не создает дубль пакета: существующий пакет обновляется по строкам и заполняет поля `Проект`, `Серийный №`, `PS`, `Наименование`, `Ед. изм.`, `Количество`, `Дата потребн.`.
- При открытии вкладки "Потребность" старый пакет с пустыми новыми полями автоматически перечитывает исходный Excel, если файл еще находится на диске.
- Для строки `IPS 1814870` из `Потребность.xlsx` теперь подтягиваются значения: `ПС200 ОДК-ПМ`, `Р24500-ВД`, `Р24500`, `КУ260305... Фланец...`, `шт`, `1`, `15.07.2026`.
- Добавлен регрессионный тест повторной загрузки файла потребности без удвоения данных.

## v2026.07.16.15-demand-view

- После команды "Загрузить потребность из ПП" приложение автоматически открывает вкладку "Потребность".
- Вкладка "Потребность" теперь показывает строки последнего загруженного пакета в составе: Проект, Серийный №, PS, Наименование, Ед. изм., Количество, Дата потребн.
- Импорт потребности читает дополнительные поля из выгрузки ПП: "Проект", "Серийный №", "Входит в станок"/PS, "Ед. изм." и "Дата потребн.".
- Исправлено автоматическое сопоставление колонок Excel: точное совпадение заголовка имеет приоритет над частичным, поэтому "Наименование" больше не подменяется колонкой "Наименование операции".
- Добавлена миграция SQLite `20260716150000_AddDemandDisplayFields` для сохранения полей Project, SerialNumber и Unit в `DemandItems`.

## v2026.07.16.14-matched-library-stock-view

- Заголовок вкладки "Библиотека" переименован в "Справочник Деталей и Норм расходов материалов и заготовок".
- Исправлен импорт файла сопоставления заказа заготовок: добавлен профиль листа "Итог" с колонками IPS, размер заготовки, вид заготовки, УТ код, наименование по 1С, материал, ед. изм. и потребность.
- Для нестандартных/поврежденных Excel-файлов добавлен резервный OpenXML-reader, если EPPlus не может открыть книгу.
- Профиль остатков 1С теперь понимает колонки "Код позиции 1С", "Наименование позиции / заготовки" и "Остаток/кол-во".
- Вкладка "Остатки" теперь показывает последний снимок "УТ остаток на дату" и таблицу выгрузки 1С УТ.

## v2026.07.16.13-library-import-export-navigation

- Импорт библиотеки перенесен во вкладку "Библиотека".
- Во вкладку "Библиотека" добавлена выгрузка текущей библиотеки в Excel.
- Верхняя команда "Импорт потребности" переименована в "Загрузить потребность из ПП" и поставлена первой.
- Верхние команды "Импорт 1С" и "Импорт остатков" объединены по интерфейсу в одну команду "Импорт остатков по 1С".
- В левом меню вкладка "Потребность" перенесена на второе место сразу после "Главная".

## v2026.07.16.12-nsi-gost-logo-fix

- Подпись на главной изменена на "Разработал Codex с участием Аракеляна А.С.".
- Логотип заменен на увеличенный черный PNG с прозрачным фоном, без темной подложки в интерфейсе.
- В редактор НСИ добавлены поля "ГОСТ материала" и "ГОСТ сортамента".
- Исправлено сохранение правки НСИ после изменения марки материала: выбранная строка сохраняется до перезагрузки списка, а ошибка сохраняется в таблицу Logs с понятным сообщением пользователю.
- Из таблицы НСИ убран столбец "Заготовка".

## v2026.07.16.11-dashboard-nsi-edit

- На главной странице добавлен фирменный блок с логотипом предприятия, фразой "Создаём, развиваем, гордимся", датой последнего обновления и подписью "Разработал Аракелян А.С. с участием ИИ".
- Карточка "Канонических заготовок" переименована в "Заготовок".
- В НСИ добавлен редактор выбранной позиции: можно изменить вид заготовки, марку материала и размер.
- При вводе размера вида `ШxВxД`, например `40x100x1030`, заготовка автоматически получает вид "Лист", если это не труба.

## v2026.07.16.10-source-name-nsi-order-logo

- Наименование заготовки в Библиотеке, выборе заготовки и НСИ теперь берется строго из выгруженного наименования 1С/НСИ (`SourceName`), а не из нормализованного имени.
- В НСИ изменен порядок колонок: Вид, Марка, Размер, Код УТ / IPS, Наименование по 1С УТ, Ед. изм., ГОСТ материала, ГОСТ сортамента, Заготовка, Источник, Обновлено.
- Колонка "Заготовка" в НСИ теперь также показывает исходное выгруженное наименование.
- Логотип в верхней панели выровнен по центру своего блока.

## v2026.07.16.9-nsi-types-usage-layout

- Добавлены виды заготовок: "Сварочный элемент", "Покупная", "Литье"; пользовательский пункт "Специальная заготовка" убран из выбора и отображения.
- Нормализация теперь относит заготовки с размерами ширина x высота x длина к виду "Лист", если это не труба.
- НСИ допускает код "УТ" или IPS в колонке "Код УТ / IPS"; резервный импорт Excel умеет подхватить IPS-код заготовки-детали с соседним наименованием.
- В окнах ввода и фильтрах добавлены явные названия полей.
- Панель "Применяемость" в НСИ получила растягиваемую ширину через разделитель между таблицей и правой панелью.
- В верхней панели оставлен только графический знак компании без текстовой части логотипа.

## v2026.07.16.8-nsi-ut-import-archive-logo

- Импорт НСИ теперь показывает понятное сообщение об итогах и включает резервный поиск УТ кода по всему листу, если Excel не имеет нормальных заголовков.
- Применяемость в НСИ привязана к выбранному УТ коду и IPS через связи библиотеки; выбор строки НСИ сделан по всей строке.
- Добавлены команды "В архив" и "Удалить" для справочника НСИ. Если позиция участвует в истории остатков, удаление переводит ее в архив.
- Добавлен логотип АО "СТП "ПЗМЦ" с сайта pzmc.org в верхнюю панель и иконку окна.
- Публикуемый файл приложения переименован в "Заказ заготовок ЦМО.exe".

## v2026.07.16.7-nsi-import-usage-fix

- Исправлен импорт НСИ из Excel без нормальных заголовков: УТ код ищется прямо в строках, наименование берется из соседней ячейки или той же строки.
- Применяемость в НСИ ищет детали не только по внутреннему идентификатору заготовки, но и по УТ коду, размеру, марке и совпадающему наименованию.
- Кнопка панели применяемости переименована в "Отобразить применяемость" / "Скрыть применяемость"; при скрытии панель освобождает место.
- В интерфейсе термин "каноническая заготовка" заменен на "заготовка".

## v2026.07.16.6-nsi-usage

- Во вкладку "НСИ" добавлена кнопка "Импортировать" для загрузки списка заготовок 1С прямо из вкладки.
- Добавлена правая боковая панель "Применяемость" с возможностью свернуть и показать.
- При выборе заготовки в НСИ справа отображаются детали из библиотеки, которые используют выбранную каноническую заготовку.

## v2026.07.16.5-nsi

- Вкладка "Нормализация НСИ" переименована в "НСИ".
- Вкладка "НСИ" показывает весь список заготовок из номенклатуры 1С: УТ код, наименование, единицу измерения, вид, размер, марку, ГОСТы, каноническую заготовку и источник.
- Добавлены фильтры по виду заготовки, размеру и марке материала, а также общий поиск по УТ коду и наименованию.

## v2026.07.16.4-library-quantity-unit

- Во вкладке "Библиотека" колонка "Количество" разделена на число и отдельную колонку "Ед. изм.".
- Редактор библиотеки принимает дробное количество через запятую: 0,1; 0,35.
- Добавлена проверка расчета дробных метров: 0,1 + 0,35 + 0,45 пог. м суммируются как 0,9 пог. м и корректно уменьшаются на складской остаток.

## v2026.07.16.3-library-filters

- Во вкладке "Библиотека" добавлены поиск по списку и фильтры по виду заготовки и единице измерения.
- Таблица библиотеки поддерживает выбор и копирование отдельных ячеек.
- Для длинномерных заготовок (круг, квадрат, трубы, уголок, швеллер, двутавр, бронзовый пруток) единица отображается как "пог. м", включая ранее импортированные связи, где была записана "шт".
- При выборе заготовки в редакторе автоматически подтягиваются УТ код и единица измерения.
- Ввод дробного количества поддерживает русскую запятую: 0,1; 0,25.
- При переходе на вкладку "Библиотека" данные автоматически подгружаются из существующей SQLite базы, без очистки накопленной библиотеки.

## v2026.07.16.2-library-edit

- Во вкладке "Библиотека" вид заготовки отображается на русском.
- Единицы измерения отображаются на русском: "шт", "пог. м".
- Добавлено редактирование существующей строки библиотеки через правую кнопку мыши -> "Редактировать".
- При редактировании выбранная строка подставляется в форму сверху, включая IPS, обозначение, наименование, заготовку, УТ код, количество, единицу и источник.

## v2026.07.16.1-library

- Вкладка "Библиотека" стала постоянным справочником применяемых материалов и заготовок.
- Добавлена ручная запись детали: IPS, обозначение, наименование, вид заготовки, заготовка, количество, единица, источник.
- УТ код подтягивается автоматически из выбранной заготовки.
- Подбор заготовки сужается по виду и введенным атрибутам, например "D60".
- Новые данные добавляются в библиотеку без удаления ранее накопленной информации.
- Старые активные связи детали не удаляются; новая ручная связь становится основной.

## v2026.07.15.1-initial-application-foundation

- Создано решение `.NET 8 / WPF / SQLite / EF Core` со слоями Core, Data, Services, Infrastructure, UI и Tests.
- Заложена доменная модель деталей, заготовок, алиасов, связей расхода, потребности, остатков, расчетов, импортов и логов.
- Добавлена начальная SQLite-схема `20260715100000_InitialCreate` с индексами для IPS, УТ/1С-кодов, дат потребности и расчетных таблиц.
- Реализованы базовые сервисы нормализации, проверки И-012, расчета потребности, конвертации единиц, поиска дублей, импорта/экспорта Excel и резервного копирования.
- Создан начальный WPF-интерфейс и первые регрессионные тесты.

Предыдущие опубликованные сборки сохранены в папке `publish` и могут использоваться для отката.









