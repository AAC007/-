# Планирование ЦМО

## v2026.08.11.1-object-card-developer-fixes — 11.08.2026 00:29

- В `Режиме разработчика` таблицы справочников сделаны явно редактируемыми для администратора; кнопки добавления, удаления и сохранения больше не зависят от случайного состояния прав текущей вкладки.
- В окне `Планирование -> Услуги на стороне` снятие галочки `Авто выбор` теперь снимает автоматически выставленные галочки с позиций, найденных в потребности.
- `Карточка объекта` в `Библиотеке` перестроена в рабочее окно с вкладками: `Информация по детали`, `Маршрут обработки и трудоемкость`, `СТО и расход`.
- Во вкладке `Информация по детали` можно править IPS, обозначение, наименование, заготовку, норму, единицу, срок, услуги и условия поставки; сохранение синхронизирует данные с Библиотекой и активной связью НСИ.
- Во вкладке маршрута добавлены строки операций с колонками `№ операции`, `Наименование`, `Рабочий центр`, `Т маш`, `Тнал`, `Твсп`; операции можно добавлять, удалять, переставлять и сохранять в маршрут обработки.
- Во вкладке `СТО и расход` можно вести инструмент/оснастку и норму расхода по операциям; данные сохраняются в рабочую SQLite-базу по IPS.
- В карточке добавлена строка `Трудоемкость изготовления` и кнопка PDF-viewer `Открыть на все окно`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 134/134 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старое предупреждение CS0162.

## v2026.08.10.3-developer-reference-lists — 10.08.2026 18:20

- В окне `Планирование -> Услуги на стороне` галочка `Авто выбор` перенесена отдельной строкой под `Только в потребности`.
- Для администратора добавлена вкладка `Режим разработчика` ниже `Настроек`: можно редактировать списки `Ед. измерения`, `Виды услуг`, `Условия поставки заготовки`, `Вид заготовок`, добавлять строки, удалять выбранные строки и сохранять изменения.
- Справочники режима разработчика сохраняются в рабочую SQLite-базу и после сохранения сразу применяются к открытым вкладкам: `Библиотека`, `НСИ`, `Подбор заготовок`, `Расчет материалов`, а также к списку видов услуг в окне услуг на стороне.
- Подписи услуг и условий поставки в редакторе `Библиотеки` теперь берутся из редактируемого справочника; отключенные позиции скрываются из формы.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 134/134 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старое предупреждение CS0162.

## v2026.08.10.2-adaptive-actions-external-services — 10.08.2026 17:10

- Верхние рабочие панели `Потребности`, `Расчета материалов`, `Библиотеки`, `НСИ`, `Подбора заготовок`, `Истории`, `Настроек` и общая верхняя панель приложения переведены на плавающую компоновку: при уменьшении окна кнопки `Удалить выделенные`, `Обновить`, `Сохранить`, `Очистить` и другие команды переносятся на новую строку, а не уходят в недоступную область.
- В редакторе `Библиотеки` блок `Условие поставки заготовки` перенесен отдельной строкой ниже блока `Услуги`, как было задано.
- Правый клик по пунктам левой навигации больше не переключает основную вкладку и не сбрасывает состояние активного окна; он только открывает контекстное меню. Окна, открытые через `Открыть в новом окне`, теперь создаются без владельца, отображаются в панели задач и не сворачиваются вместе с главным окном.
- В окне `Планирование -> Услуги на стороне` галочка выбора строки работает с первого клика. При включении `Только в потребности` колонка `Переместить` заполняется количеством по реальной потребности, но не больше доступного НЗП; новая галочка `Авто выбор` дополнительно сама отмечает такие строки, сохраняя возможность ручной правки.
- Из окна `Услуги на стороне` убраны кнопка `Отметить по списку` и поле `Список IPS/кодов для отметки`, чтобы выбор выполнялся через таблицу и автоматический фильтр потребности.
- Добавлен регрессионный тест на расчет количества услуг по потребности без учета остатка `на склад` и с ограничением по доступному НЗП.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 134/134 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старое предупреждение CS0162 по недостижимому хвосту экспорта заявки услуг.

## v2026.08.10.1-login-layout-library-supply-services — 10.08.2026 12:00

- В окне входа и первичной настройки администратора восстановлен нормальный русский текст; нажатие `Enter` теперь выполняет действие `Войти`/`Применить` без наведения мышью на кнопку.
- На `Главной` убран общий скролл; верхние редакторы `Библиотеки` и `НСИ` переведены на плавающую компоновку, поэтому кнопки `Очистить`, `Сохранить` и `Сохранить правку НСИ` переносятся на доступную строку при уменьшении окна.
- В `Библиотеке` добавлен атрибут условия поставки заготовки: `ТО` и `Лазерная резка`. Значение хранится в базе по детали, отображается колонкой рядом с материалом и выгружается в `Библиотека.xlsx`.
- В контекстное меню строки `Библиотеки` добавлена команда `Карточка объекта` перед открытием PDF. Карточка показывает сведения по детали, условие поставки, заготовку и встроенный PDF-viewer, а также разделы `2 маршрут обработки и трудоемкость` и `3 Оснащение и расход`.
- В окне `Планирование -> Услуги на стороне` заголовок заменен на `Выберите продукцию и вид услуги для отправки на сторону`; создание перемещения 1С больше не закрывает окно, чтобы после этого можно было сформировать Excel-заявку.
- Комментарий для перемещения услуг на стороне теперь группируется по IPS и добавляет IPS перед номерами станков: `Услуги на стороне: Хим окс IPS 1326628 Р26655 - 4 шт; ...`.
- Добавлена миграция SQLite `20260810120000_AddPartBlankSupplyRequirements` и регрессионные тесты на сохранение/выгрузку условий поставки и новый формат комментария услуг.
- Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 133/133 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старое предупреждение CS0162 по недостижимому хвосту экспорта заявки услуг.

## v2026.08.05.3-nsi-1c-mm-units — 05.08.2026 20:54

- В `НСИ -> Добавить из 1С` расширен текстовый поиск: ввод вроде `Круг д60 сталь 20` нормализуется в понятный для 1С вариант `Круг D60 сталь 20`, при этом кодовый поиск по УТ/IPS сохранен.
- Окно поиска 1С перед каждым новым поиском очищает старый список и сбрасывает прошлый выбор, чтобы в результатах не оставалась позиция из предыдущего запроса.
- Кнопка выбора позиции в окне 1С переименована с `Выбрать в редактор` на `Добавить в НСИ`.
- При добавлении/сохранении НСИ цифровой 11-значный IPS из 1С теперь показывается и хранится в пользовательском виде без ведущих нулей: `00001371148` -> `1371148`; УТ-коды не меняются.
- В списках видов заготовок убраны `Пруток бронзовый`, `Лист бронзовый`, `Швеллер`, `Двутавр`; `Сварочный элемент` переименован в `Сварное изделие`. Старые строки с удаленными видами остаются читаемыми и не удаляются из базы.
- Для длинномерных норм расхода в `Библиотеке` добавлен пользовательский режим `мм`: можно внести `62 мм`, в базе и для расчетов/заявок/комплектации это сохраняется как `0,062 пог. м`. Остатки длинномерных позиций в НСИ отображаются в миллиметрах.
- Добавлены регрессионные тесты на поиск `д60` и на сохранение нормы `мм -> пог. м`.
- Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 133/133 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.05.3-nsi-1c-mm-units`; `VERSION.md` скопирован в папку версии, ярлык меню Пуск `Планирование ЦМО.lnk` обновлен, ПО запущено из новой публикации.

## v2026.08.05.2-search-users-library-demand-filter — 05.08.2026 12:10

- Расширен общий поиск в окнах ПО: цифровой код с ведущими нулями вроде `00001414993` теперь находит строку с IPS `1414993`; составной ввод вроде `04.067.02.003 Гайка плоская` ищется по объединенным полям строки, поэтому не нужно отдельно искать только обозначение или только наименование.
- Новое правило поиска применено к основным спискам: `Потребность`, `Библиотека`, `Планирование`, `Подбор заготовок` и `Расчет материалов`.
- В `Настройках` добавлена команда удаления пользователя. Удаление выполняется как деактивация учетной записи: пользователь исчезает из списка входа, но история входов и данные аудита сохраняются. Защита не дает удалить текущего пользователя и последнего активного администратора.
- В `Библиотеке` рядом с фильтром `Связь` добавлена галочка `В потребности`; она оставляет в списке только детали из последней загруженной потребности и обновляет инфостроку.
- Добавлены регрессионные тесты на поиск с ведущими нулями, составной поиск по обозначению и наименованию, фильтр библиотеки `В потребности` и удаление пользователя.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 132/132 тестов; остались штатные предупреждения NU1701 по PdfiumViewer, NU1900 по недоступному NuGet vulnerability feed и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.05.2-search-users-library-demand-filter`; ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на новый exe, ПО запущено из опубликованной папки.

## v2026.08.05.1-login-context-menu-crash-fix — 05.08.2026 10:21

- Исправлена авария после успешного ввода пароля: главное окно не открывалось из-за `XamlParseException` на `MenuItem` контекстного меню навигации (`Unable to cast object of type 'System.Windows.Controls.MenuItem' to type 'System.Windows.Controls.ContentControl'`).
- Контекстное действие `Открыть в новом окне` для вкладок главного меню перенесено из XAML-стиля в программное создание меню по правому клику, поэтому загрузка `MainWindow` больше не ломается.
- Пароли, пользователи и база авторизации не изменялись; по логу вход проходил успешно, падение происходило уже при создании главного окна.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 129/129 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.05.1-login-context-menu-crash-fix`, ПО запущено из новой папки. Обновление ярлыка меню Пуск не удалось: Windows/окружение отказало в сохранении `Планирование ЦМО.lnk`, старый ярлык остался на предыдущей версии.

## v2026.08.04.12-ui-polish-new-windows — 04.08.2026 18:21

- На `Главной` исправлена подсветка карточек-счетчиков `Деталей`, `Заготовок`, `Без заготовки`, `Без МСК`, `Дубли НСИ`: hover теперь идет по тому же скругленному контуру, что и сама карточка, без синего прямоугольного пятна.
- Кнопки основного окна приведены к единому виду: одинаковая минимальная высота, овальная форма, единая рамка, hover/pressed-состояния и аккуратные отступы.
- Во вкладке `Настройки` визуальные параметры больше не сбрасываются при повторном входе во вкладку; выбор шрифта и размера применяется сразу к окну, положение навигации также остается в выбранном состоянии.
- После сохранения прав пользователя выбранная учетная запись остается открытой, а при изменении текущего пользователя его права обновляются в активной сессии.
- Во вкладке `Планирование` добавлена галочка `В потребности`: список сокращается до IPS из последнего загруженного пакета потребности.
- Для пунктов главного меню добавлено контекстное действие правой кнопкой `Открыть в новом окне`, чтобы одновременно держать открытыми несколько вкладок.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 129/129 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.12-ui-polish-new-windows`; ярлык меню Пуск `Планирование ЦМО.lnk` обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.11-settings-permissions-navigation — 04.08.2026 17:45

- В `Настройках` исправлена таблица прав пользователя: общий стиль `DataGrid` больше не блокирует галочки, права `Чтение` и `Редактирование` переключаются обычными чекбоксами в ячейках.
- Окно `Настройки` уплотнено без общего вертикального скролла: вид приложения и учетная запись расположены в две колонки сверху, управление пользователями и правами занимает основную нижнюю область.
- Добавлена отдельная настройка положения вкладок `Главная`, `Потребность`, `Расчет материалов` и т.д.: `Слева`, `Справа`, `Сверху`, `Снизу`. При выборе сверху/снизу вкладки становятся горизонтальными.
- Настройка положения вкладок сохраняется в `Settings` как `UI.NavigationPlacement` и применяется при загрузке программы; прежняя настройка верхних кнопок сохранена отдельно.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 129/129 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.11-settings-permissions-navigation`; ярлык меню Пуск обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.10-login-user-list — 04.08.2026 17:30

- В окне входа поле `Логин` заменено на редактируемый выпадающий список активных пользователей: можно выбрать существующий логин из списка или набрать вручную.
- После успешного входа приложение сохраняет только логин последнего пользователя в настройке `Auth.LastUserName`; пароль не сохраняется и по-прежнему проверяется только через bcrypt-хэш.
- При следующем запуске последний пользователь автоматически подставляется в окно входа, чтобы не вводить логин заново.
- Добавлены регрессионные тесты: список входа содержит только активных пользователей, успешный вход обновляет последний логин.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 129/129 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.10-login-user-list`; ярлык меню Пуск обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.9-login-main-window-start — 04.08.2026 17:21

- Исправлено закрытие приложения сразу после успешного ввода логина и пароля: WPF завершал программу при закрытии окна входа, потому что действовал стандартный режим `OnLastWindowClose`.
- На время окна входа приложение теперь работает в режиме явного завершения, а после успешной авторизации назначает `MainWindow` главным окном и возвращает закрытие по главному окну.
- По логу подтверждено: вход проходил успешно, данные главного окна начинали грузиться, затем приложение штатно уходило в `Application is shutting down` без исключения.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 127/127 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.9-login-main-window-start`; ярлык меню Пуск обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.8-auth-sqlite-login-fix — 04.08.2026 17:16

- Исправлена ошибка входа после нажатия `Применить` в первичной настройке администратора и при повторном входе: SQLite не мог выполнить LINQ-запрос с `DateTime.UtcNow.Subtract(...)` внутри фильтра защиты от brute-force.
- Расчет границы 15-минутного окна блокировки теперь выполняется до SQL-запроса и передается в SQLite обычным параметром, поэтому создание первого администратора сразу завершает вход без аварии.
- Добавлен регрессионный тест на реальном SQLite-провайдере: первичная настройка администратора выполняет создание пользователя и вход без исключений. Старые InMemory-тесты такую проблему не ловили.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 127/127 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.8-auth-sqlite-login-fix`; ярлык меню Пуск обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.7-login-window-layout — 04.08.2026 17:09

- Исправлено окно первичной настройки администратора: окно стало больше, его можно растянуть, а содержимое формы прокручивается при малой высоте экрана или крупном масштабе Windows.
- Поля `Логин`, `Отображаемое имя`, `Пароль` и `Повторите пароль` получили достаточную высоту и внутренние отступы, поэтому значения вроде `admin` и `Администратор` больше не отображаются обрезанными.
- Кнопки действия закреплены в нижней части окна и остаются видимыми после ввода пароля; для первичной настройки основная кнопка теперь называется `Применить`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 126/126 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162.
- Публикация выполнена в `publish/versions/v2026.08.04.7-login-window-layout`; ярлык меню Пуск обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.6-auth-rbac — 04.08.2026 16:07

- Добавлен вход в приложение через учетную запись. При первом запуске без пользователей открывается первичная настройка администратора; без успешного входа основное окно не открывается.
- Добавлены таблицы `AppUsers`, `AppUserPermissions`, `AuthLoginAttempts`: пароли не хранятся в открытом виде, используется `BCrypt.Net-Next` с work factor 12; неудачные попытки входа журналируются с логином, причиной, машиной и временем.
- Добавлена защита от brute-force: после 5 неудачных попыток входа за 15 минут вход для этого логина временно блокируется.
- Во вкладке `Настройки` администратор может создавать пользователей, генерировать пароль, сбрасывать пароль и назначать права `Чтение` / `Редактирование` по вкладкам. Обычный пользователь в `Настройках` может сменить собственный пароль.
- Навигация фильтруется по правам чтения; на вкладках без права редактирования кнопки действий основного окна отключаются. Для одновременной работы нескольких устройств SQLite-подключение переведено на `Cache=Shared`, при запуске включаются `PRAGMA journal_mode=WAL` и `busy_timeout=5000`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 126/126 тестов; остались штатные предупреждения NU1701 по PdfiumViewer и старые CS0162 по недостижимому коду в методах услуг.
- Публикация выполнена в `publish/versions/v2026.08.04.6-auth-rbac`; проверено наличие `Планирование ЦМО.exe`, `x64/pdfium.dll`, `VERSION.md`. Ярлык меню Пуск `Планирование ЦМО.lnk` обновлен, ПО открыто из опубликованной папки.

## v2026.08.04.5-workshop-report — 04.08.2026 15:29

- Добавлена вкладка `Отчет` сразу после `Планирование`: отчет механического цеха формируется за выбранный период в деловом формате для генерального директора, технического директора или совета директоров.
- Производственные показатели считаются из рабочих данных: план выпуска - по последней потребности за период, факт выпуска и номенклатура - по read-only отчету 1С о перемещениях из 44 секции на `1 секция (ПЗМЦ) только для производственных целей` и `Покраска ТМЦ`, выполнение плана - как отношение факта к плану.
- Добавлены KPI по OEE/загрузке на основе рассчитанного плана вкладки `План`, численность персонала план/факт через API ПО "Производство" при доступности, а простои, ремонты, аварии, ФОТ, материалы и запчасти оставлены ручными полями без выдумывания значений.
- Кнопка `Обновить 1С и сформировать` запускает read-only скрипт `onec_integration/tools/build_onec_ut_reports_odata.py` за выбранные даты; локальный JSON берется только при совпадении периода, чтобы в отчет не попадали цифры чужого месяца.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 124/124 теста; остались штатные предупреждения NU1701/NU1900 по NuGet/PdfiumViewer и старые CS0162 по недостижимому хвосту методов услуг.
- Публикация выполнена в `publish/versions/v2026.08.04.5-workshop-report`; проверено наличие `Планирование ЦМО.exe`, `x64/pdfium.dll`, `VERSION.md`. Ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на эту версию, ПО открыто из опубликованной папки.

## v2026.08.04.4-planning-viewer-services-request — 04.08.2026 15:16

- Во вкладке `Планирование` во встроенный PDF-viewer добавлена кнопка `Открыть на все окно`: текущий найденный чертеж открывается в отдельном максимизированном окне просмотра.
- В окне `Услуги на стороне` добавлен фильтр `Только в потребности`; при включении остаются только позиции НЗП, по которым есть строки последней потребности, а строки сверх потребности `на склад` не считаются основанием для фильтра.
- В Excel-заявке на услугу дата теперь считается от даты формирования заявки: `Азотирование` +10 дней, остальные услуги +7 дней. Если эта расчетная дата позже исходной даты потребности детали, дата и примечание подсвечиваются красным жирным, в колонку `Примечание` записывается `СРОЧНО!`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 124/124 теста; остались штатные предупреждения NU1701/NU1900 по NuGet/PdfiumViewer и старые предупреждения CS0162 по недостижимому хвосту методов услуг.
- Публикация выполнена в `publish/versions/v2026.08.04.4-planning-viewer-services-request`; проверено наличие `Планирование ЦМО.exe`, `x64/pdfium.dll`, `VERSION.md`. Ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на эту версию, ПО открыто из опубликованной папки.

## integration-2026.08.04-onec-ut-reports — 04.08.2026 13:08

- Освоен read-only путь отчетов из 1С УТ через OData для будущего внедрения в ПО: перемещения из `44 секция НЗП` на `1 секция (ПЗМЦ) только для производственных целей` и `Покраска ТМЦ`; списание материалов из 44 секции "сырье -> детали" по документам `КомплектацияНоменклатуры`.
- Добавлен инструмент `onec_integration/tools/build_onec_ut_reports_odata.py`, который строит JSON и CSV за период. По умолчанию берутся только проведенные документы (`Posted=true`); непроведенные можно включить только флагом `--include-unposted` для диагностики.
- Проверено на живом OData: за `2026-08-04` найдено 4 строки перемещений; за `2026-08-01..2026-08-04` найдено 8 строк перемещений и 4 строки проведенных списаний материалов. `python -m py_compile onec_integration/tools/build_onec_ut_reports_odata.py` прошел. Публикация WPF-версии не выполнялась.

## v2026.08.04.3-library-service-flags — 04.08.2026 12:04

- В карточку детали в Библиотеке добавлены признаки услуг на стороне: `Азотирование`, `ТО`, `Хим. окс`, `Шпон паз`; признаки сохраняются в SQLite и переживают обновления ПО.
- Во вкладке `Библиотека` добавлена колонка `Примечание`, где выбранные признаки услуги отображаются одной строкой, например `Азотирование; ТО`.
- В редакторе Библиотеки галочки услуг размещены под IPS/обозначением/наименованием; при команде `Редактировать` признаки подтягиваются из выбранной строки, при `Сохранить` записываются в карточку детали.
- Окно `Услуги на стороне` во вкладке `Планирование` теперь показывает только детали из НЗП 44 секции, у которых в Библиотеке отмечена хотя бы одна услуга; при выборе вида услуги список дополнительно фильтруется по соответствующей галочке.
- Добавлена миграция `20260804010000_AddPartExternalServiceFlags`; новые поля в `Parts`: `RequiresNitriding`, `RequiresHeatTreatment`, `RequiresChemicalOxidation`, `RequiresKeyway`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 123/123 теста; остались штатное предупреждение NU1701 по `PdfiumViewer` и старые предупреждения CS0162 по недостижимому хвосту в методах услуг.
- Публикация выполнена в `publish/versions/v2026.08.04.3-library-service-flags`; проверено наличие `Планирование ЦМО.exe` и `x64/pdfium.dll`.

## v2026.08.04.2-nsi-material-gost-autofill — 04.08.2026 00:45

- В редакторе НСИ выпадающий список `Марка материала` теперь строится только по активным позициям НСИ с активным алиасом; материалы из канонических/библиотечных заготовок без строки НСИ больше не показываются.
- Добавлена автоподстановка `ГОСТ материала`: если введенная марка материала совпадает с существующей маркой из активной НСИ, поле заполняется самым частым ГОСТом для этой марки.
- Добавлена автоподстановка `ГОСТ сортамента`: при выборе вида заготовки, кроме `Покупная`, поле заполняется самым частым ГОСТом сортамента для этого вида из активной НСИ.
- Ручной ввод и выбор через выпадающий список сохранены: если пользователь изменил ГОСТ вручную, автоподстановка его не перетирает.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 122/122 теста; остались штатное предупреждение NU1701 по `PdfiumViewer` и технические предупреждения CS0162 от старого недостижимого хвоста в методах услуг.
- Публикация выполнена в `publish/versions/v2026.08.04.2-nsi-material-gost-autofill`; ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на новую папку, ПО запущено из опубликованной версии.

## v2026.08.04.1-planning-detail-metrics-sqlite-fix — 04.08.2026 00:22

- Исправлена ошибка в правой колонке `Информация по детали` во вкладке `Планирование`: SQLite больше не получает `SUM(decimal)`, количества потребности читаются списком и суммируются в приложении.
- Добавлен регрессионный тест `Msk_detail_metrics_sums_decimal_demand_on_client_for_sqlite`, который проверяет выбор детали с decimal-потребностью и отсутствие ошибки `SQLite cannot apply aggregate operator`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 120/120 тестов; осталось штатное предупреждение NU1701 по `PdfiumViewer`.
- Публикация выполнена в `publish/versions/v2026.08.04.1-planning-detail-metrics-sqlite-fix`; ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на новую папку, ПО запущено из опубликованной версии.

## v2026.08.03.7-planning-info-services-allocation — 03.08.2026 19:20

- Во вкладке `Планирование` окно `Информация по детали` разделено на две колонки: слева остались IPS, обозначение, наименование и данные МСК/библиотеки, справа теперь показываются `В потребности сейчас`, `В производстве` и `Доступно для запуска`.
- В окне `Услуги на стороне` список выбора больше не дублирует один IPS под разные проекты: показывается одна строка НЗП по IPS/коду с общим доступным количеством.
- При формировании Excel-заявки на услугу выбранное количество раскрывается по строкам последней потребности: каждая строка получает свои `ПС`, `№ станка`, количество и дату; если выбранного количества больше, чем текущая потребность по IPS, остаток добавляется отдельной строкой `на склад`.
- Комментарий для перемещения услуг на стороне строится по той же раскрытой логике: только вид услуги и пары `№ станка; количество`, включая строку `на склад` для остатка сверх потребности.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 119/119 тестов; остались предупреждение NU1701 по `PdfiumViewer` и два технических предупреждения CS0162 от старого недостижимого хвоста в методах услуг.
- Публикация выполнена в `publish/versions/v2026.08.03.7-planning-info-services-allocation`; ярлык меню Пуск `Планирование ЦМО.lnk` обновлен на новую папку, ПО запущено из опубликованной версии.

## v2026.08.03.6-nsi-delete-undo — 03.08.2026 18:50

- Исправлена отмена случайного удаления в НСИ: после удаления позиции команда `Отменить` теперь восстанавливает удаленную строку НСИ обратно в базу и список.
- Восстановление возвращает не только алиас и каноническую заготовку, но и связанные карты применяемости, ссылки остатков и ссылки старых расчетов, которые были затронуты удалением.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 119/119 тестов.

## v2026.08.03.5-external-services-request-stock-filter — 03.08.2026 18:18

- Во вкладке `Подбор заготовок` рядом с подбором добавлена галочка `С положительным остатком`: при включении список показывает только подходящие заготовки, по которым в последнем снимке остатков есть количество больше 0. Сопоставление остатков выполняется по нормализованному УТ/IPS-коду.
- Окно `Услуги на стороне` теперь связывает детали из 44 секции НЗП с последней потребностью: в списке видны `ПС`, `№ станка` и `Дата потребности`, а комментарий формируется коротко по принципу запуска в производство: вид услуги и пары `№ станка; количество`.
- В окне `Услуги на стороне` добавлена кнопка `Сформировать заявку`. Она создает Excel-бланк с колонками `№`, `ПС`, `№ станка`, `Код IPS`, `Номенклатура`, `Ед. измерения`, `Кол-во`, `Обоснование`, `Дата потребности`, `Цена, руб`, `Стоимость, руб`.
- Перемещение 1С по услугам группирует выбранные строки по коду номенклатуры, проверяет целое количество в `шт` и не дает переместить больше остатка 44 секции НЗП.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 118/118 тестов; `python -m py_compile onec_integration/tools/create_goods_transfer_odata.py` прошел.

## v2026.08.03.4-external-services-transfer — 03.08.2026 16:06

- Во вкладке `Планирование` добавлена кнопка `Услуги на стороне`. Она открывает окно выбора деталей из последнего НЗП именно по `44 секция НЗП (незавершенное производство)`.
- В окне можно выбрать вид услуги (`Хим окс`, `Азотирование`, `Шпоночный паз`, `Термообработка`), вставить список IPS/кодов для автоматической отметки строк или отметить детали вручную, указать количество к перемещению и комментарий.
- При подтверждении программа создает непроведенный документ 1С `ПеремещениеТоваров` из `44 секция НЗП (незавершенное производство)` в `Детали МУ в обработке на стороне` через уже проверенный OData-контур `create_goods_transfer_odata.py`; запись выполняется только с явным `--allow-write`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 114/114 тестов; `python -m py_compile onec_integration/tools/create_goods_transfer_odata.py` прошел.

## v2026.08.03.3-cmo-blank-stock-calculation — 03.08.2026 11:46

- Исправлен расчет материалов для заготовок, которые лежат в `44 секция НЗП (незавершенное производство)`: если остаток найден по УТ-коду активной заготовки, он теперь вычитается из закупочной потребности так же, как складской остаток.
- Проверен сценарий IPS `1657364` / УТ `УТ000022134`: потребность 8 шт и остаток 6 шт в 44 секции дают к закупке 2 шт, а в окне расчета показывается только непокрытый хвост по следующему № станка, а не все строки потребности.
- Раскладка строк в окне `Расчет материалов` и в Excel-заявке теперь списывает остаток заготовки сверху вниз по строкам потребности и показывает только непокрытые строки. Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 112/112 тестов.

## integration-2026.08.03-onec-goods-transfer — 03.08.2026 11:45

- Освоено создание документа 1С `ПеремещениеТоваров` через OData. Пробно создан непроведенный документ `УТПЗ0010222`, `Posted=false`, отправитель `44 секция НЗП (незавершенное производство)`, получатель `Детали МУ в обработке на стороне`, организация `СТП ПЗМЦ АО`, флаги управленческого, бухгалтерского и налогового учета включены.
- Добавлен инструмент `onec_integration/tools/create_goods_transfer_odata.py`: запись разрешается только с `--allow-write`, пустой пробный документ только с `--empty-trial`, реальные строки перемещения требуют явного `--item "<код>" "<количество>"`.
- Проверено: документ прочитан обратно через OData; `python -m py_compile onec_integration/tools/create_goods_transfer_odata.py` прошел. Публикация WPF-версии не выполнялась, код приложения не менялся.

## v2026.08.03.2-piecewise-comment-preview — 03.08.2026 11:34

- В окне `Запустить в производство` при включенной галочке `Запуск по 1 шт` поле комментария теперь показывает построчный предпросмотр: каждая строка соответствует отдельной комплектации 1С и содержит текст, который будет записан именно в этот документ.
- Если пользователь вручную редактирует многострочный список, каждая строка передается в соответствующую комплектацию без пересборки в общий комментарий. Если оставить одну строку, она используется как префикс, а программа сама дописывает № станка и `1 шт`.
- Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 110/110 тестов.

## v2026.08.03.1-piecewise-production-launch — 03.08.2026 11:19

- Во вкладке `Планирование` в окне `Запустить в производство` добавлена галочка `Запуск по 1 шт`: при включении программа создает отдельные комплектационные карты 1С по одной детали, а не один общий документ на все количество.
- Поштучный запуск автоматически распределяет документы по текущему дефициту и № станков: если строка потребности требует 2 детали, создаются две карты с комментарием вида `Создано ПО Планирование Р26666; 1 шт`, затем программа переходит к следующему станку.
- Обычный режим запуска сохранен без изменений; расчет доступного количества по остаткам материала и учет реза 5 мм для `пог. м` продолжают применяться. Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 109/109 тестов.

## v2026.07.31.12-branch3-production-launch-merge — 31.07.2026 15:08

- Последняя версия ПО объединена с веткой `(3)`: в рабочие исходники перенесены вкладка `План`, `Дефицит ЦМО`, блок `План -> Персонал`, клиент API ПО "Производство", настройки `PZMC_PRODUCTION_*`, APS-модели/сервисы и тесты ветки `v2026.07.31.10-pzmc-personnel-api-routes`.
- Поверх ветки `(3)` сохранены свежие правки последней версии: команда `Запустить в производство` во вкладке `Планирование`, учет реза 5 мм для `пог. м`, учет склада + НЗП/ЦМО для доступного материала, короткий редактируемый комментарий и OData-создание комплектации 1С.
- Сохранены исправленные реквизиты комплектации 1С: ответственный `Аракелян Армен Седракович`, подразделение `Цех механической обработки`, единицы измерения из `ЕдиницаХраненияОстатков_Key`. Проверено: `dotnet test BlankDemandPlanner.sln --no-restore` прошел, 108/108 тестов; `python -m py_compile onec_integration/tools/create_production_assembly_odata.py` прошел.

## v2026.07.31.11-production-launch-requisites-fix — 31.07.2026 14:09

- В создании комплектации 1С исправлены закрепленные реквизиты: ответственный теперь `Аракелян Армен Седракович`, подразделение всегда `Цех механической обработки`.
- Исправлена ошибка единиц измерения `<Объект не найден>` в комплекте и комплектующих: для документа берется `ЕдиницаХраненияОстатков_Key` номенклатуры 1С, с резервом на `ЕдиницаДляОтчетов_Key`, а не базовая единица измерения.
- Уже созданный документ `УТПЗ0013771` отремонтирован в 1С: снята пометка удаления, документ остался непроведенным (`Posted=false`), реквизиты и единицы измерения заменены на корректные. Проверено: `python -m py_compile onec_integration/tools/create_production_assembly_odata.py` и `dotnet test BlankDemandPlanner.sln` прошли, 100/100 тестов.

## v2026.07.31.10-production-launch-odata-filter-fix — 31.07.2026 13:52

- Исправлена ошибка создания комплектации 1С `HTTP 500: Операция не разрешена в предложении ГДЕ ... sourceAlias.Code AUTOORDER`: OData-поиск номенклатуры для запуска в производство больше не строит длинный `$filter` с `or`, а выполняет простые точечные запросы по `Code` и `Артикул`.
- Параметры OData-запросов теперь кодируются через `%20`, без `+` в выражениях фильтра. Повторный запуск по сохраненному payload IPS `1375701` успешно создал непроведенный документ `УТПЗ0013771`, `Posted=false`, компонент `УТ000022660` `9,481 пог. м`.

## v2026.07.31.9-production-launch-cut-comment — 31.07.2026 13:22

- В запуске в производство для заготовок `пог. м` добавлен учет реза 5 мм между деталями: материал для 1С считается как `норма * количество + 0,005 * (количество - 1)`, а доступное количество запуска ограничивается с учетом этого реза.
- Комментарий комплектации 1С сокращен до списка закрываемых станков и количества: `Создано ПО Планирование Р26704; 1 шт Р26689; 1 шт`. Из комментария убраны IPS, обозначение/наименование детали, расход компонента, количество деталей, текст `не проведено`, проекты/ПС и даты.
- В окне запуска поле комментария теперь доступно для ручного редактирования перед созданием документа 1С. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 100/100 тестов.

## v2026.07.31.8-production-launch-wip-stock — 31.07.2026 13:06

- В запуске в производство исправлен расчет доступного материала: для выбранной детали материал теперь считается не только по группе `Склад`, но и по `НЗП/ЦМО`, включая `44 секция НЗП (незавершенное производство)`, потому что комплектация создается через 44 секцию.
- Проверено на сценарии IPS `1347282`: заготовка `Круг D40 Сталь 40Х ГОСТ 2590-2006 / ГОСТ 4543-2016`, остаток `9,965 пог. м`, норма `0,06 пог. м` дают доступный запуск `166` деталей, а не сообщение о нехватке материала. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 100/100 тестов.

## v2026.07.31.7-production-launch — 31.07.2026 12:55

- Во вкладку `Планирование` добавлена кнопка `Запустить в производство`: после выбора детали МСК открывается окно с расчетом доступного количества запуска по остатку материала и активной норме расхода из Библиотеки.
- В окне можно выбрать `Сделать сколько можно` или указать конкретное количество, при этом количество не может быть больше обеспеченного материалом. Комментарий автоматически показывает, какие проекты и номера станков закрываются запуском по текущему дефициту последней потребности.
- Добавлен сервис и OData-скрипт создания документа 1С `КомплектацияНоменклатуры`; документ создается непроведенным (`Posted=false`), чтобы не выполнять складское списание без отдельного подтверждения. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 99/99 тестов; `py_compile` нового скрипта прошел.

## v2026.07.31.6-left-nav-fit-labels — 31.07.2026 10:20

- В левой колонке навигации пункт `Расчет материалов` больше не обрезается: колонка получила достаточную ширину, внутренний отступ пункта уменьшен, принудительное многоточие убрано.
- Скроллинг в левой колонке остается отключенным, логотип и верхняя шапка не менялись. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.31.5-logo-only-left-header — 31.07.2026 10:05

- Из левого верхнего блока рабочей панели убрана версия ПО рядом с логотипом: в этой зоне теперь остается только графический знак `PZMC_1.png`.
- Правило закреплено в проектной памяти: рядом с логотипом нельзя добавлять версию, slug версии или текстовое название приложения. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.31.4-left-nav-no-scroll — 31.07.2026 09:55

- В левой колонке вкладок полностью отключены вертикальная и горизонтальная прокрутка: список разделов остается статичным без скроллинга. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.31.3-compact-left-navigation — 31.07.2026 09:40

- Левая колонка вкладок (`Главная`, `Потребность`, `Расчет материалов` и далее) сделана уже и плотнее, чтобы рабочие заголовки и основное содержимое начинались ближе к меню.
- Верхний отступ списка вкладок оставлен на уровне начала рабочей строки, а длинные названия защищены от выхода за пределы колонки. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.31.2-logo-window-title-fix — 31.07.2026 09:28

- Возвращен логотип `PZMC_1.png` в левый верхний угол рабочей панели: знак больше не убирается при показе версии.
- Версия убрана из системного заголовка окна; в самом верхнем углу Windows теперь остается только `Планирование ЦМО`, а компактная версия без slug показывается рядом с логотипом в рабочей панели. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.31.1-header-version-navigation — 31.07.2026 09:14

- В верхнем левом углу рабочей панели убран малый логотип и длинное техническое имя версии; теперь показывается компактная строка `Планирование ЦМО vYYYY.MM.DD.N` без slug, например `Планирование ЦМО v2026.07.31.1`.
- Заголовок окна использует тот же компактный формат `Планирование ЦМО vYYYY.MM.DD.N`, а боковая колонка навигации прижата ближе к рабочему заголовку с единым левым отступом. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.30.3-planning-cmo-nsi-suggestions — 30.07.2026 15:58

- ПО переименовано в `Планирование ЦМО`: обновлены заголовок окна, метаданные сборки и системные заголовки ошибок; это имя закреплено в правилах проекта для следующих обновлений.
- В `Расчете материалов` поиск заготовки для разового добавления теперь строится только по активным строкам НСИ (`BlankAliases`), а не по каноническим заготовкам. Поэтому в выпадающий список больше не должны попадать позиции, которых пользователь не видит в составе НСИ.
- При публикации обновления создается актуальный ярлык меню Пуск `Планирование ЦМО.lnk`, указывающий на exe текущей опубликованной версии. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.30.2-nsi-onec-calc-blank-attributes — 30.07.2026 15:51

- Во вкладке `Нормативно-справочная информация` убрана кнопка `Импортировать`: рабочее добавление НСИ теперь идет через `Добавить из 1С`, чтобы пользователь выбирал реальную позицию 1С и дозаполнял недостающие атрибуты.
- В `Расчете материалов` поиск заготовки для разового назначения переделан по аналогии с `Подбором заготовок`: добавлены поля `Вид заготовки`, `Материал`, `D`, `Ширина`, `Высота`, `Толщина`, `Стенка`, `Длина` и дополнительный поиск по НСИ.
- Кнопка `Добавить заготовку` сохранена и по-прежнему добавляет заготовку только в текущий расчет, без записи в Библиотеку. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 98/98 тестов.

## v2026.07.30.1-blank-pick-global-search — 30.07.2026 14:47

- Исправлен подбор заготовок по диаметру для поковок: `Поковка` с размером `D...` теперь участвует в подборе как круглая заготовка, поэтому при вводе `D=400` предлагаются подходящие `D450` и `D660`, а сортировка остается от меньшего припуска к большему.
- Глобальный поиск теперь применяется к активным рабочим вкладкам с поиском: `Потребность`, `Расчет материалов`, `Библиотека`, `НСИ`, `Планирование`, `Остатки`; в расчете материалов добавлено собственное поле поиска по IPS, детали, УТ-коду, виду и номенклатуре.
- Текстовые поля поиска и ввода получили единый скругленный стиль, чтобы верхние поисковые зоны вкладок выглядели ближе к единому варианту из `Подбора заготовок`. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 97/97 тестов.

## v2026.07.29.9-library-explicit-edit — 29.07.2026 18:10

- Исправлена опасная автозагрузка редактора Библиотеки: обычный клик/выделение ячейки больше не переносит данные строки в поля редактора.
- Редактор Библиотеки теперь заполняется только явной командой `Редактировать` через контекстное меню строки; выделение строки остается доступным для удаления, открытия чертежа и копирования ячеек. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 96/96 тестов.

## v2026.07.29.8-app-icon-symbol — 29.07.2026 16:32

- В левом верхнем углу верхней панели старый бледный логотип заменен на предоставленный знак `PZMC_1.png`.
- Из `PZMC_1.png` создан многоразмерный `PZMC_1.ico`; он назначен как `Window.Icon` и `ApplicationIcon`, чтобы значок окна, exe и ярлык меню Пуск использовали один и тот же вложенный файл. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 95/95 тестов.

## v2026.07.29.7-blank-selection-pick-fix — 29.07.2026 13:55

- Исправлен выпадающий список `Поиск детали без заготовки` во вкладке `Подбор заготовок`: теперь он показывает только видимые активные карточки Библиотеки без активной заготовки и не подтягивает архивные/удаленные позиции вроде `1223371 | ОЦ-80.002 Пробка центровая`.
- Исправлено сохранение выбора при подборе: если пользователь выбрал конкретную заготовку в результатах, повторное нажатие `Подобрать` не сбрасывает выбор на первую верхнюю строку. Выпадающий список деталей больше не использует общий обработчик редактируемых ComboBox, который мог сбивать выбранную деталь. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 95/95 тестов.

## v2026.07.29.6-library-delete-stays-deleted — 29.07.2026 13:33

- Исправлено удаление объектов из Библиотеки: команда `Удалить` теперь убирает карточку детали из видимого списка целиком, деактивирует ее активные связи с заготовками и архивирует саму карточку, а не оставляет ее сразу же строкой `без заготовки`.
- Ручное удаление получает отдельную метку `[MANUAL_LIBRARY_DELETE]`, поэтому автодобавление из актуальной потребности больше не возвращает такую карточку при обычном обновлении/открытии Библиотеки. Старые архивные детали без этой метки по-прежнему могут возвращаться при импорте/актуализации потребности. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 93/93 тестов.

## v2026.07.29.5-official-logo — 29.07.2026 11:40

- Исправлен внешний вид брендового блока на главной странице: самодельная синяя плашка удалена, вместо нее используется предоставленный фирменный файл `PZMC_.png` с логотипом и полным названием завода.
- Текст `Создаём, развиваем, гордимся`, дата/версия ПО и информация о разработчике оставлены справа от фирменного логотипа с увеличенным отступом. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 92/92 тестов.

## v2026.07.29.4-main-version-logo — 29.07.2026 11:29

- Главное окно и карточка на главной странице больше не используют жестко зашитую старую версию: актуальная версия и дата берутся из первой записи `VERSION.md`, поэтому после публикации обновления версия автоматически меняется и в шапке окна, и на главной.
- Блок `Создаём, развиваем, гордимся` сдвинут правее от логотипа, а логотип на главной сделан ярким: добавлена цветная плашка ПЗМЦ с контрастным знаком и подписью. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 92/92 тестов.

## v2026.07.29.3-startup-demand-restore — 29.07.2026 10:47

- Добавлена стартовая загрузка всех основных вкладок после открытия главного окна: Библиотека теперь применяет восстановление деталей из актуальной потребности сразу при запуске ПО, а не только после ручного нажатия `Обновить` или перехода во вкладку.
- Это дополняет исправление для IPS `1816772`: скрытая архивная карточка детали возвращается в активную Библиотеку при запуске, при импорте потребности и при открытии Библиотеки. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 92/92 тестов.

## v2026.07.29.2-restore-demand-archived-parts — 29.07.2026 10:43

- Исправлен реальный сценарий с IPS `1816772`: если деталь ранее была удалена из Библиотеки и скрыта архивной меткой `[ARCHIVED_LIBRARY]`, новая актуальная потребность теперь возвращает такую карточку в рабочую Библиотеку без заготовки.
- Импорт потребности сразу восстанавливает найденную архивную карточку детали, а открытие Библиотеки дополнительно сверяет актуальные строки потребности и возвращает скрытые детали, которые снова пришли как нормальные детали.
- Сообщение после импорта потребности теперь считает прирост активных деталей Библиотеки, включая восстановленные из архива карточки, а не только совершенно новые IPS. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 92/92 тестов.

## v2026.07.29.1-demand-selection-pdf-combos — 29.07.2026 10:31

- Автодобавление новых деталей из потребности усилено: цифровые IPS из потребности с полноценным обозначением и наименованием автоматически попадают в Библиотеку, включая пример `1816772 / 270.011.02.001 Вал задней бабки`; операции и сборки по-прежнему отсекаются.
- Во вкладке `Потребность` включено множественное выделение мышью с удержанием левой кнопки для строк/ячеек.
- Во вкладке `Подбор заготовок` справа добавлена панель информации по детали с встроенным PDF-viewer по аналогии с `Планированием`; при выборе детали без заготовки открывается быстрый поиск чертежа через локальный кэш/папки/IPS Bridge.
- Исправлено закрепление выбранной подсказки в редактируемых выпадающих списках; подсказки материалов и ГОСТов в НСИ берутся из активных вариантов НСИ и ищутся без учета регистра. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 91/91 тестов. Остаются ожидаемые предупреждения NU1701 для `PdfiumViewer` и NU1900 при недоступном индексе NuGet.

## v2026.07.28.25-cached-onec-prices — 28.07.2026 18:32

- Убрана онлайн-загрузка цен при обычном открытии/редактировании НСИ: вкладка теперь читает цену из локального SQLite-кэша `OneCPriceItems`.
- Цены 1С обновляются пакетно вместе с общей командой `Обновить данные 1С` и при запуске программы в том же фоновом контуре, что и остатки.
- Формирование Excel-заявки также берет цены из локального кэша, чтобы не тормозить экспорт прямым запросом в 1С. Добавлена миграция SQLite `20260728182500_AddOneCPriceItems`. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 90/90 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.24-request-line-total-price — 28.07.2026 18:22

- На листе `Заявка` в Excel-файле закупа колонка `Цена` теперь считается аналогично листу `По группе`: это стоимость строки, а не цена одной единицы.
- Расчет выполняется как `Кол-во к закупке * цена за шт` для `шт` и `Кол-во к закупке * цена за пог. м` для `пог. м`; лист `По группе` сохраняет итоговую стоимость верхнего уровня и строк деталей.
- Обновлен регрессионный тест экспорта: пример `0,85 пог. м * 100 ₽` дает `85 ₽`, а `0,465 пог. м * 100 ₽` дает `46,5 ₽`. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 90/90 тестов. Остаются ожидаемые предупреждения NU1701 и временно NU1900 при проверке NuGet.

## v2026.07.28.23-group-total-price — 28.07.2026 18:16

- При формировании Excel-заявки на листе `По группе` верхняя строка группы теперь показывает итоговую стоимость закупки, а не цену одной единицы.
- Расчет цены группы выполняется как `Всего Купить * цена за шт` для `шт` и `Всего Купить * цена за пог. м` для `пог. м`; строки деталей внутри раскрытия также показывают стоимость своей строки.
- Лист `Заявка` продолжает показывать цену за единицу после даты потребности заготовки. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 90/90 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.22-blank-selection-part-details — 28.07.2026 18:12

- Во вкладке `Подбор заготовок` добавлено окно `Информация по детали`, максимально повторяющее окно информации из `Планирования`.
- При выборе детали в поле `Поиск детали без заготовки` окно показывает IPS, обозначение, наименование, состояние заготовки, материал, УТ-код, норму, срок, наличие в библиотеке и МСК.
- Если по выбранному IPS есть данные МСК, атрибуты заготовки подтягиваются оттуда; если данных нет, показывается `Заготовка: Не подобрана`.
- Добавлен регрессионный тест на заполнение информации по выбранной детали. Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 90/90 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.21-request-price-group-outline — 28.07.2026 18:04

- В Excel-файле заявки на лист `Заявка` добавлена колонка `Цена` сразу после `Дата потребности заготовки`.
- На листе `По группе` добавлена колонка `Цена` после `Всего Купить`.
- Лист `По группе` теперь группируется по УТ-коду/IPS и единице измерения: верхняя строка показывает общий итог закупки, а строки деталей под ней скрыты и раскрываются стандартной группировкой Excel.
- Цена для заявки подтягивается через уже подключенный read-only источник цен 1С УТ; при недоступности 1С экспорт не падает, а оставляет цену пустой.
- Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 89/89 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.20-nsi-price-source-planning-fix — 28.07.2026 17:50

- Подключен read-only источник цен НСИ из 1С УТ: пакетный скрипт `resolve_nomenclature_prices.py` ищет активные УТ/IPS-коды и читает последние ненулевые цены из регистра `ЦеныНоменклатуры`, с предпочтением типа цены `Стоимость`.
- В таблице НСИ вместо двух колонок `Цена за пог. м / шт` и `Валюта` теперь одна колонка `Цена`; валюта объединяется с числом, например `100 ₽`.
- Исправлено падение вкладки `Планирование` при клике по объекту и вводе поиска: таблица больше не привязывает `SelectedItem` в режиме выбора ячеек, поэтому WPF не пытается выбирать целую строку.
- Проверено: `dotnet test BlankDemandPlanner.sln` прошел, 89/89 тестов. Точечный read-only запрос 1С по `УТ000011939` вернул цену `7483.33`, валюту `руб.`, тип цены `Стоимость`. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.19-nsi-price-columns — 28.07.2026 17:45

- В таблицу НСИ после колонки `Склад` и перед `Источник` добавлены колонки `Цена за пог. м / шт` и `Валюта`.
- Строка НСИ расширена полями цены и валюты; пока источник цен не подключен, значения остаются пустыми и не влияют на расчет.
- Добавлена проверка загрузки НСИ с новыми полями. `dotnet test BlankDemandPlanner.sln` прошел, 88/88 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.18-meter-cut-display-fix — 28.07.2026 17:35

- В `Расчете материалов` исправлено отображение количества для `пог. м`: строка детали теперь показывает чистую потребность по норме из Библиотеки, без добавления 5 мм реза в каждую видимую строку.
- Правило реза сохранено в итоговом расчете: `TotalRequired`, вычитание складского остатка и итог `Купить` продолжают учитывать 0,005 м между отрезками одной заготовки.
- В Excel-заявке лист `Заявка` показывает построчные количества без добавленного реза, а лист `По группе` суммирует закупочную потребность уже с учетом реза и остатков.
- Добавлены регрессионные тесты на разделение нормы строки и итогового расчета с резом. `dotnet test BlankDemandPlanner.sln` прошел, 88/88 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.17-planning-cell-copy — 28.07.2026 17:15

- Во вкладке `Планирование` таблица переведена в ячеечное выделение: клик выбирает конкретную ячейку, а не всю строку.
- Убрано принудительное выделение всей строки при клике по объекту планирования; информация по детали и открытие PDF по клику сохраняются через выбранную строку.
- Ctrl+C в `Планировании` теперь подчиняется общему правилу приложения: копируются только фактически выделенные ячейки, без всей строки и без заголовков.
- `dotnet test BlankDemandPlanner.sln` прошел, 87/87 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.28.16-library-blank-prefix-search — 28.07.2026 17:05

- В поиске заготовки в редакторе Библиотеки ввод `Круг D1` больше не трактуется как точный диаметр 1 мм. Короткий размер теперь работает как префикс и показывает подходящие позиции вроде `Круг D100`, `Круг D115`, `Круг D140`.
- Для составного ввода поиск дополнительно сверяет отдельные слова/атрибуты, чтобы комбинации вида `Круг D1 Сталь` находили заготовку по типу, размеру и материалу одновременно.
- Добавлен регрессионный тест на подбор заготовок по префиксу диаметра. `dotnet test BlankDemandPlanner.sln` прошел, 87/87 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.15-copy-selected-cells-only — 28.07.2026 16:50

- Закреплено правило копирования таблиц: Ctrl+C копирует только фактически выделенные ячейки, без подтягивания всей строки и без заголовков.
- В главном окне добавлен единый обработчик команды `Copy`, который формирует буфер из `DataGrid.SelectedCells` в порядке строк и колонок.
- Правило внесено в `AGENTS.md` и `PROJECT_CONTEXT.md`, чтобы дальнейшие доработки не возвращали стандартное копирование строк `DataGrid`.
- `dotnet test BlankDemandPlanner.sln` прошел, 86/86 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.14-hide-duplicate-key-column — 28.07.2026 16:45

- В НСИ убрана пользовательская колонка `Ключ дубля`; группировка дублей по сходимости сохранена.
- Статус фильтра `Только дубли` теперь сообщает, что строки сгруппированы по сходимости, без ссылки на отдельную колонку.
- `dotnet test BlankDemandPlanner.sln` прошел, 86/86 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.13-nsi-duplicates-combo-fix — 28.07.2026 16:40

- В НСИ фильтр `Только дубли` теперь показывает понятную сходимость: добавлена колонка `Ключ дубля`, а строки сортируются группами по физическому ключу заготовки. Для примера с `D90`, `D150`, `D230` каждая пара теперь стоит рядом со своим ключом, а не перемешивается по УТ-коду.
- Статус и инфострока НСИ при показе дублей выводят количество групп дублей и подсказывают смотреть колонку `Ключ дубля`.
- Исправлено закрепление выбора во всплывающих списках ручного ввода: при выборе подсказки текст принудительно записывается в поле. Это применено к марке материала и ГОСТам НСИ, а также к редактируемым спискам выбора заготовки/детали в рабочих вкладках.
- Добавлен регрессионный тест на группировку дублей НСИ. `dotnet test BlankDemandPlanner.sln` прошел, 86/86 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.12-context-pdf-menu — 28.07.2026 16:30

- В контекстное меню строк во вкладках "Потребность", "Расчет материалов" и "Библиотека" добавлена команда `Открыть чертеж (PDF)`.
- В "Библиотеке" команда открытия чертежа расположена перед `Редактировать`, чтобы сначала можно было быстро проверить PDF выбранной детали.
- Открытие чертежей использует общий путь поиска: стабильный локальный кэш, папки `Данные для работы\Чертежи`/`drawings`, дополнительные настроенные папки и затем IPS Bridge при локальном промахе.
- `dotnet test BlankDemandPlanner.sln` прошел, 85/85 тестов. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.11-nsi-delete-duplicates — 28.07.2026 16:05

- Исправлена ошибка удаления НСИ через контекстное меню: WPF иногда передавал служебный объект вместо строки, из-за чего команда редактирования/контекстного меню падала при перерисовке таблицы. Команды НСИ теперь безопасно принимают `object?`, достают строку сами и не падают на служебных параметрах.
- Удаление НСИ дополнительно обернуто в обработку ошибок: при сбое пользователь получает понятное сообщение, а подробности пишутся в журнал приложения.
- Пересмотрена логика `Дубли НСИ`: раньше она группировала по `NormalizedSourceName`, поэтому могла показывать позиции, совпавшие только по слишком общей нормализованной строке. Теперь дубли определяются по физическому ключу заготовки: вид, единица, материал, ГОСТы и точные размеры; если размеров нет, используется полное имя, но не одна марка материала.
- Главная карточка `Дубли НСИ`, фильтр `Только дубли` в НСИ и сервис `DuplicateDetectionService` используют один и тот же строгий ключ.
- Добавлены регрессионные тесты на безопасную команду контекстного меню НСИ и на то, что одинаковый материал при разных размерах не считается дублем.
- `dotnet test BlankDemandPlanner.sln` прошел, 85/85 тест. Остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.10-search-normalization — 28.07.2026 15:40

- Ручной поиск в рабочих вкладках переведен на единое нормализованное сравнение: кириллица ищется без учета регистра, а похожие русские/латинские буквы в кодах и обозначениях приводятся к одному виду.
- Примеры теперь работают одинаково: `01-р014` находит обозначения `01-P014...`, `напра` и `Напра` ищут по наименованиям без разного результата из-за регистра.
- Нормализация подключена к поиску в "Потребности", "Библиотеке", "НСИ", "Остатках", "Планировании", подбору деталей без заготовок и подсказкам заготовок. В окне `Добавить из 1С` похожий на код ввод уходит в 1С уже с нормализованной латинской буквой.
- Добавлены регрессионные тесты на смешение русской `р` и латинской `P`, а также на поиск в библиотеке по строчному русскому вводу.
- `dotnet test BlankDemandPlanner.sln` прошел, 83/83 тест. Публикация выполнена в `publish/versions/v2026.07.28.10-search-normalization`; остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.9-onec-button-refreshes-nsi-names — 28.07.2026 15:20

- Кнопка `Обновить данные 1С` теперь после успешной синхронизации остатков принудительно запускает пакетную сверку активных УТ/IPS-кодов НСИ с реальной 1С УТ и обновляет наименования заготовок из 1С.
- Сообщение после ручного обновления 1С показывает, сколько наименований НСИ было обновлено, либо что сверка прошла без изменений.
- Добавлен регрессионный тест на повторное обновление имени НСИ из 1С после того, как вкладка НСИ уже открывалась в текущем сеансе.
- `dotnet test BlankDemandPlanner.sln` прошел, 81/81 тест. Публикация выполнена в `publish/versions/v2026.07.28.9-onec-button-refreshes-nsi-names`; остаются ожидаемые предупреждения NU1900 и NU1701 для `PdfiumViewer`.

## v2026.07.28.8-nsi-live-1c-names — 28.07.2026 15:00

- Заголовок открытой вкладки НСИ изменен на `Нормативно-справочная информация`; пункт меню `НСИ` оставлен коротким.
- При первом открытии НСИ в сеансе приложение пакетно сверяет активные УТ/IPS-коды с реальной 1С УТ и обновляет наименование заготовки из 1С, если оно отличается от ранее импортированного из Excel.
- Актуализация после импорта НСИ также переведена на пакетный read-only запрос 1С вместо последовательных запросов по одной строке.
- Добавлен скрипт `onec_integration/tools/resolve_nomenclature_codes.py` для read-only получения фактических наименований 1С по списку кодов за один COM-сеанс.
- Живой read-only тест по `УТ000022886` подтвердил фактическое имя из 1С: `Круг D115 Сталь 40Х ГОСТ 2590-2006 / ГОСТ 4543-2016`.
- Добавлен регрессионный тест на обновление имени НСИ из 1С при загрузке вкладки; `dotnet test BlankDemandPlanner.sln` прошел, 80/80 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.7-planning-title-msk-buttons — 28.07.2026 14:50

- Уточнено переименование МСК: `Планирование` оставлено только как название раздела/вкладки и заголовок окна.
- Рабочие кнопки, подсказки, статусы, колонка наличия данных и карточка главной страницы возвращены к привычному названию `МСК`: `Обновить данные МСК`, `Открыть Excel МСК`, `Без МСК`, `Есть МСК`.
- Правило фильтра IPS из предыдущей версии сохранено: в Библиотеку из МСК по-прежнему добавляются только 7- или 11-значные цифровые IPS, мусорные строки не создают детали.
- `dotnet test BlankDemandPlanner.sln` прошел, 79/79 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.6-planning-ips-filter — 28.07.2026 14:35

- Вкладка `МСК` переименована в `Планирование`: обновлены пункт меню и заголовок раздела.
- Автодобавление из планирования в Библиотеку теперь допускает только нормальный цифровой IPS: 7 или 11 цифр. Короткие номера вроде `463`, дефисы, текстовые обозначения и строки `ZAGOTOVKA...` больше не создают детали в Библиотеке.
- При загрузке/обновлении планирования выполняется очистка ранее ошибочно созданных автозаписей `Source = МСК` с невалидным IPS; ручные записи и нормальные IPS не удаляются.
- Рабочая база `Данные для работы\BlankDemandPlanner.db` очищена сразу: удалено 17 ошибочных автозаписей планирования, включая примеры `-`, `463`, `ZAGOTOVKA...`, текстовые шайбы/опоры и похожие строки; связанные карты расхода удалены, строки потребности не затронуты.
- Добавлен регрессионный тест на фильтр IPS и очистку ошибочных автозаписей; `dotnet test BlankDemandPlanner.sln` прошел, 79/79 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.5-onec-fast-stock-sync — 28.07.2026 14:15

- В синхронизацию остатков 1С добавлен OData-first путь по принятой рекомендации обследования: приложение сначала пробует read-only `standard.odata`, формирует тот же `StockSnapshot`, а при ошибке или сомнительном результате автоматически переходит на COM.
- Для OData добавлен отдельный скрипт `onec_integration/tools/sync_stock_sections_odata.py`: учетные данные берутся только из внешнего `.env`, URL с кириллическими сущностями кодируется корректно, диагностический JSON сохраняется отдельно.
- Чтобы пользователь не ждал нестабильный OData на полном снимке, WPF ограничивает быстрый канал 20 секундами и сразу переключается на резервный COM-запрос.
- Резервный COM-скрипт ускорен: вместо отдельного запроса по каждой секции теперь выполняется 2 пакетных запроса к 1С, отдельно по группе `НЗП/ЦМО` и по группе `Склад`.
- Живой read-only прогон пакетного COM по закрепленным секциям 1С прошел успешно: `7770` строк, `358` НЗП/ЦМО, `7412` склад, `0` ошибок, около `31` секунды на холодном старте.
- `dotnet test BlankDemandPlanner.sln` прошел, 78/78 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.4-msk-library-autosync — 28.07.2026 13:20

- При обновлении МСК из `X:\19_МЕХ УЧАСТОК\База МСК\СПИСОК МСК` новые IPS, которых еще нет в Библиотеке, автоматически создаются как карточки деталей с источником `МСК`.
- Если по новой детали МСК найден УТ-код заготовки и он есть в активном НСИ, приложение сразу создает связь детали с этой заготовкой, единицей и нормой расхода из МСК.
- Если ранее удаленная/архивная карточка детали снова обнаружена в МСК, она возвращается в рабочую библиотеку с источником `МСК`.
- После обновления МСК статус показывает количество автоматически добавленных деталей, а при ненулевом количестве выводится уведомление пользователю.
- Добавлен регрессионный тест на автоматическое добавление IPS из МСК в Библиотеку и привязку активной заготовки из НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 78/78 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.3-nsi-editor-suggestions — 28.07.2026 12:55

- В редакторе НСИ исправлен выбор марки материала из выпадающего списка: выбранное значение закрепляется в поле редактора и сохраняется вместе с записью.
- Подсказки марки материала теперь ищут без учета регистра для кириллицы: ввод `сталь` находит значения вида `Сталь 40Х`.
- Поля `ГОСТ материала` и `ГОСТ сортамента` переведены на редактируемые выпадающие списки: можно выбрать существующий ГОСТ из подсказки или ввести новое значение вручную.
- Верхняя кнопка `Удалить` рядом с `Добавить из 1С` убрана; удаление НСИ оставлено через правую кнопку мыши в таблице.
- После сохранения новой или отредактированной позиции НСИ статус показывает `Деталь добавлена в список: <код>` и больше не затирается перезагрузкой списка.
- Добавлен регрессионный тест на регистронезависимые подсказки НСИ и сохранение статуса; `dotnet test BlankDemandPlanner.sln` прошел, 77/77 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## analysis-2026.07.28-1c-odata-stock — 28.07.2026

- Проверен отдельный OData endpoint 1С `http://pz-sql1/ut_dev/odata/standard.odata/`: сервис доступен, требует авторизацию и публикует справочники `Номенклатура`, `Склады`, регистры `ТоварыНаСкладах`, `ПартииТоваровНаСкладах`, `ТоварыОрганизаций`.
- Подтверждено, что стандартный OData предоставляет функцию `Balance` для `AccumulationRegister_ТоварыНаСкладах`, поэтому остатки можно получать через виртуальную таблицу, а не через чтение движений.
- Практический тест `УТ000011445` + `44 секция НЗП` вернул остаток `3.549`, совпадающий с ранее полученным COM-helper результатом по основному регистру.
- Решение закреплено в `onec_integration/docs/ODATA_STOCK_ANALYSIS.md`: текущий прогретый COM-helper остается основным для одиночных оперативных запросов, OData готовить как ускоряющий read-only канал для холодного старта, пакетных запросов и синхронизации с локальным кэшем GUID и fallback на COM.

## v2026.07.28.2-nsi-1c-lookup-delete — 28.07.2026 12:20

- В НСИ рядом с импортом добавлена кнопка `Добавить из 1С`: открывается окно поиска по УТ-коду, IPS/коду 1С или части наименования, пользователь выбирает позицию из реальной 1С УТ, после чего код, наименование и единица измерения попадают в редактор НСИ; вид, размер, материал и ГОСТы можно дозаполнить сразу или позже.
- При импорте НСИ приложение дополнительно сверяет импортированные УТ/IPS-коды с реальной 1С УТ и обновляет наименование заготовки из 1С, чтобы изменения номенклатуры в 1С попадали в справочник ПО.
- Кнопка `В архив` удалена из НСИ, а логика автоматического возврата архивных НСИ отключена: теперь позиция либо есть в рабочем списке, либо удалена. Удаление выделенных строк физически убирает выбранные алиасы/заготовки из НСИ.
- Если удаляемая НСИ используется в библиотеке деталей, приложение показывает предупреждение с количеством активных связей и требует подтверждение; после подтверждения активные связи с деталями удаляются, а исторические строки остатков/расчетов не блокируют удаление.
- Добавлен read-only контур поиска номенклатуры 1С через `onec_integration/tools/search_nomenclature.py` и WPF-сервис `OneCNomenclatureService`; `dotnet test BlankDemandPlanner.sln` прошел, 76/76 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.28.1-library-msk-pdf-demand-sync — 28.07.2026 11:03

- В "Библиотеке" незаполненные строки теперь подсвечиваются цветом `#FFCC00`: без активной заготовки, без активного УТ-кода/алиаса или с нулевой нормой расхода.
- В МСК правая панель "Информация по детали" расширена: внутренний вертикальный скроллинг карточки убран, чтобы текст помещался в рабочую область вкладки.
- Исправлена причина, по которой чертежи МСК часто не открывались: после локального поиска приложение снова использует read-only `IPS Bridge 3.3.1` по алгоритму ПО "Производство" (`login`, `search_by_string`, `get_object`/`get_structure`, `export_file_attribute`, `read_staged_file_chunk`) и сохраняет найденный PDF в стабильный кэш `IPS_<код>.pdf`; если роль IPS не задана в конфиге, выбирается первая доступная роль.
- Открытие PDF в МСК остается только по живому клику строки/ячейки или команде открытия, без реакции на простое наведение мыши.
- При импорте потребности счетчик новых деталей теперь считается после синхронизации библиотеки, а повторное обновление существующей партии не пропускает новые строки: новые IPS деталей создаются в библиотеке устойчиво.
- При создании детали из новой потребности библиотека сверяет МСК по IPS и, если в МСК есть УТ-код, ищет его в активном НСИ; найденная заготовка, единица и норма расхода сразу закрепляются за деталью.
- Добавлены регрессионные тесты на подсветку незаполненной строки библиотеки и открытие PDF через bridge после локального промаха; `dotnet test BlankDemandPlanner.sln` прошел, 76/76 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного индекса NuGet и NU1701 для `PdfiumViewer`.

## analysis-2026.07.28-blank-excel-merge — 28.07.2026

- Объединены Excel-файлы заготовок из `Данные для работы` в итоговую книгу `reports/blank_excel_merge_20260728/Итоговая_библиотека_заготовок.xlsx`.
- Добавлен pandas-скрипт `tools/merge_blank_excel_files.py` для повторяемой обработки: сопоставление разных заголовков, нормализация материала, ГОСТов, размеров, числовых норм и единиц измерения; колонка `Источник файла` в итоговой книге проставляется активной гиперссылкой.
- Итоговая книга содержит листы `Итог` и `Удаленные дубли`; дедупликация выполнена по `IPS`, так как в проекте IPS является основным идентификатором детали.
- После подтверждения правил пользователем закреплены: десятичный разделитель `0,047`, пустые текстовые поля без `-`, приоритет актуальности источников и правило размеров `L` только для штучных заготовок; для `пог. м` длина идет в норму расхода. Если основной итоговый файл открыт в Excel, скрипт создает новую книгу с суффиксом времени.
- Код WPF-приложения не менялся, сборка и публикация не выполнялись.

## v2026.07.27.9-msk-click-pdf-live-selection — 27.07.2026 18:10

- В МСК PDF-чертеж больше не ищется и не открывается от наведения мыши или простого изменения выделения: карточка детали обновляется при выборе строки, а открытие PDF запускается живым кликом по строке/ячейке или командой открытия.
- Ускорен локальный поиск PDF: приложение больше не перечисляет тяжелую сетевую папку исходных Excel МСК на `X:\...`, а проверяет точные соседние PDF и локальные папки чертежей/кэш.
- Из правой панели "Информация по детали" в МСК убрана строка `Файл`; исходный Excel по-прежнему открывается штатной командой.
- В "Подборе заготовок" поиск стал активным при вводе: изменение вида, материала или размеров автоматически обновляет список подходящих заготовок без обязательного нажатия кнопки "Подобрать".
- Добавлены регрессионные тесты на отсутствие автооткрытия PDF при выборе строки МСК, скрытие строки `Файл` в карточке и live-подбор заготовок; `dotnet test BlankDemandPlanner.sln` прошел, 75/75 тестов. Остаются ожидаемые предупреждения NU1900 из-за недоступного NuGet и NU1701 для `PdfiumViewer`.

## v2026.07.27.8-msk-click-nsi-cut-width — 27.07.2026 17:42

- В МСК убрана реакция строк на наведение мыши: детальная информация и PDF меняются только после клика/выбора строки.
- Для встроенного PDF-viewer-а добавлен перехват WinForms-исключений, включая ошибку `PdfiumViewer.PdfDocument.GetPageLinks` из Microsoft .NET/JIT-окна; ошибка пишется в лог и не должна закрывать ПО.
- Подбор заготовок теперь берет только заготовки из активного НСИ: позиции без активного алиаса НСИ не попадают в результат подбора.
- В расчете материалов для `пог. м` добавлена ширина реза 5 мм (`0,005 м`) между отрезками одной заготовки. Пример `0,1 + 0,1 + 0,03` теперь считается как `0,24`; складской остаток вычитается из потребности с учетом реза.
- Окно расчета и Excel-заявка используют ту же раскладку реза, что и основной расчет. Добавлены регрессионные тесты для реза и фильтра НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 74/74 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.27.7-msk-pdfium-native-fix — 27.07.2026 15:35

- Исправлено закрытие ПО при переходе между объектами МСК: `PdfiumViewer` падал из-за недоступной native-библиотеки `pdfium.dll`.
- При запуске ПО в путь поиска native-библиотек добавляются папка приложения и подпапка `x64`; при сборке и публикации `pdfium.dll` теперь копируется и в корень сборки, и в `x64`.
- Инициализация встроенного PDF-viewer-а защищена: если viewer не запустился или PDF не открылся, вкладка МСК показывает причину в статусе вместо аварийного закрытия приложения.
- `dotnet test BlankDemandPlanner.sln` выполнен после правки; остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.27.6-msk-local-pdf-no-bridge — 27.07.2026 15:28

- Из вкладки "МСК" убрана кнопка "Обновить PDF", чтобы пользовательский путь больше не уходил на старый IPS Bridge.
- Открытие PDF-чертежа в МСК переведено на локальный поиск: сначала стабильный кэш `%LOCALAPPDATA%\BlankDemandPlanner\IpsDrawings\IPS_<код>.pdf`, затем PDF рядом с файлом МСК, папки `Данные для работы\Чертежи`, `drawings`, известная папка интеграции с чертежами и дополнительный путь из `BLANK_DEMAND_DRAWINGS_DIR`.
- Если PDF найден, но встроенный viewer не смог его открыть, причина теперь показывается в статусе МСК, а не теряется в логе.
- Добавлена регрессионная проверка, что команда открытия чертежа использует локальный PDF и не вызывает bridge; `dotnet test BlankDemandPlanner.sln` прошел, 72/72 тестов. Остается ожидаемое предупреждение NU1701 для `PdfiumViewer`.

## v2026.07.27.5-msk-pdfium-viewer — 27.07.2026 17:05

- Вкладка "МСК" переведена с WebView2 на встроенный PdfiumViewer через `WindowsFormsHost`, по аналогии с ПО "Производство", где используется `PdfiumViewer.dll`.
- Быстрый просмотр теперь открывает уже полученный PDF напрямую из локального файла/кэша без запуска браузерного движка WebView2; при смене детали предыдущий документ освобождается, чтобы файл не блокировался.
- Пакет WebView2 удален из UI-проекта; добавлены `PdfiumViewer` и `PdfiumViewer.Native.x86_64.v8-xfa`. `dotnet test BlankDemandPlanner.sln` прошел, 72/72 тестов; остается предупреждение NU1701 из-за старого .NET Framework-пакета PdfiumViewer.

## v2026.07.27.4-ui-demand-msk-pdf-viewer — 27.07.2026 16:20

- В "Библиотеке" исправлен сбой при правом клике по строке: контекстное меню больше не пытается принудительно выбрать всю строку в режиме ячеечного выделения.
- В детализации "Потребности" исправлен расчет дефицита по повторяющимся IPS: дефицит считается как общее количество деталей минус исходное количество "В производстве"; в заголовок добавлено значение "в производстве".
- Верхние формы, фильтры и редакторы приведены к единому размеру полей и кнопок; кнопки получили единое скругление.
- Во вкладке "МСК" добавлен встроенный PDF-viewer на WebView2. PDF-чертеж сначала открывается из стабильного локального кэша `%LOCALAPPDATA%\BlankDemandPlanner\IpsDrawings\IPS_<код>.pdf`, а IPS Bridge вызывается только при отсутствии кэша или по кнопке "Обновить PDF".
- Добавлены регрессионные проверки дефицита потребности и быстрого открытия PDF из кэша; `dotnet test BlankDemandPlanner.sln` прошел, 72/72 тестов.

## v2026.07.27.3-dashboard-library-import-fix — 27.07.2026 14:18

- На главной панели исправлены актуальные счетчики: `Деталей` считается по активным строкам библиотеки без архивных карточек, `Заготовок` - по активным строкам НСИ, `К закупке` - только по последнему расчету; карточка `Позиций 1С` убрана.
- Карточка `Дубли НСИ` теперь открывает вкладку `НСИ` с включенным фильтром `Только дубли`; этот фильтр также доступен в самой вкладке НСИ.
- Импорт библиотеки теперь распознает 10-колоночный файл вида `IPS / Обозначение / Наименование / Заготовка / УТ-код / Материал / ГОСТ материала / ГОСТ сортамента / Ед. / Количество`, поэтому пример `1410079` обновляет актуальную заготовку `Заготовка 50х105х1630мм Сталь 30ХГСА`, код `УТ000013591`, единицу `шт` и количество `2`.
- При явном импорте библиотеки очищается старый след `Удалено из библиотеки ...` в источнике детали, чтобы удаленная архивная строка не выглядела как актуально восстановленная запись.
- Добавлены регрессионные проверки счетчиков главной, фильтра дублей НСИ и нового формата импорта; `dotnet test BlankDemandPlanner.sln` прошел, 70/70 тестов.

## v2026.07.27.2-msk-drawing-click-open — 27.07.2026 15:35

- Во вкладке `МСК` открытие PDF-чертежа теперь запускается отдельным кликом по строке/ячейке, а не зависит только от смены выделения при наведении.
- Если автоматический поиск при наведении уже пробовал IPS и не открыл чертеж, клик по строке выполняет повторную принудительную попытку через `IPS Bridge 3.3.1`.
- Добавлена регрессионная проверка, что команда открытия чертежа повторно обращается к bridge при клике; `dotnet test BlankDemandPlanner.sln` прошел, 68/68 тестов.

## v2026.07.27.1-msk-ips-drawing-auto-open — 27.07.2026 15:10

- Во вкладке `МСК` из правой панели `Информация по детали` убрана верхняя сводная строка с табличными значениями; карточка теперь сразу начинается с подробных полей `IPS`, `Обозначение`, `Наименование` и далее.
- Из основной таблицы `МСК` убрана колонка `Файл`; путь к исходному Excel МСК остается в детальной карточке и в команде открытия.
- Подключен read-only клиент `IPS Bridge 3.3.1`: при наведении на строку МСК приложение ищет PDF-чертеж по IPS через MCP bridge, сохраняет найденный файл в локальный кэш `%LOCALAPPDATA%\BlankDemandPlanner\IpsDrawings` и автоматически открывает PDF.
- Параметры IPS Bridge читаются из `config.toml`/`ips_bridge_config.toml`, переменных окружения или внешнего контура `Интеграция с сервисами`; секреты не записываются в репозиторий и не выводятся в статусах.
- Добавлена регрессионная проверка карточки МСК без сводной строки; `dotnet test BlankDemandPlanner.sln` прошел, 67/67 тестов.

## v2026.07.24.6-msk-import-nsi-usage-strict — 24.07.2026 15:35

- Исправлен импорт `Данные из МСК.xlsx` для фактического 11-колоночного формата: IPS, обозначение, наименование детали, вид, полное наименование заготовки, материал, УТ-код, ГОСТ материала, ГОСТ сортамента, единица и норма расхода.
- Для примера `1320756 / КПТ-04.00.001А01` импорт теперь берет заготовку `Круг D80 Сталь 40Х ГОСТ 2590-2006 / ГОСТ 4543-2016`, код `УТ000022657`, единицу `пог. м` и норму `0,065`, а не подставляет `Круг` и `1`.
- Окно применяемости в НСИ теперь показывает только детали с активной связью именно на выбранную заготовку, без похожих деталей по материалу, размеру или названию.
- Добавлены регрессионные проверки нового формата МСК и строгой применяемости НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 67/67 тестов.

## v2026.07.24.5-nsi-blank-import-restore — 24.07.2026 15:05

- Уточнено восстановление после импорта МСК: если УТ-код заготовки был перенесен в архив НСИ, импорт `Данные из МСК.xlsx` теперь возвращает в работу и алиас НСИ, и саму каноническую заготовку.
- При повторном использовании нормализованной заготовки импорт также активирует ранее архивированную каноническую позицию, чтобы она снова участвовала в поиске, подсказках и строках библиотеки.
- Добавлена регрессионная проверка импорта МСК поверх архивной позиции НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 66/66 тестов.

## v2026.07.24.4-library-import-restore — 24.07.2026 14:45

- Исправлено восстановление `Библиотеки` после удаления всех строк: повторный импорт `Данные из МСК.xlsx` теперь снимает архивную метку с ранее удаленных IPS и возвращает строки в список.
- При открытии `Библиотеки` добавлено самовосстановление уже импортированных строк: если у архивной детали появилась активная заготовка, карточка снова отображается без повторного импорта.
- Добавлены регрессионные проверки импорта МСК поверх архивной детали и загрузки библиотеки после уже выполненного импорта; `dotnet test BlankDemandPlanner.sln` прошел, 65/65 тестов.

## v2026.07.24.3-library-delete-fix — 24.07.2026 14:20

- В "Библиотеке" исправлено удаление через правую кнопку мыши: правый клик теперь выбирает строку под курсором, а команда `Удалить` идет через общий механизм удаления строк.
- Исправлено удаление выделенных строк в режиме ячеечного выделения: команда продолжает собирать строки из выделенных ячеек и корректно архивирует выбранные объекты.
- Исправлен случай деталей без активной заготовки, у которых уже есть старые архивные карты заготовок: такая строка теперь действительно убирается из библиотеки и отвязывается от строк потребности.
- Добавлена регрессионная проверка удаления детали без активной заготовки при наличии неактивных карт; `dotnet test BlankDemandPlanner.sln` прошел, 63/63 теста.

## v2026.07.24.2-msk-headerless-library-import — 24.07.2026 13:55

- В окне "Расчет материалов" кнопка `Сделать заявку Bitrix 24` переименована в `Сформировать заявку`.
- Импорт библиотеки теперь распознает файл `Данные из МСК.xlsx` без строки заголовков: колонки читаются как IPS, обозначение, наименование детали, вид/размер/материал заготовки, норма, единица, УТ-код и наименование заготовки.
- Для такого МСК-формата добавлен отдельный профиль `MskHeaderlessBlankImportProfile`, поэтому импорт обновляет детали, заготовки, УТ-коды, нормы расхода и единицы измерения в библиотеке.
- Добавлена регрессионная проверка импорта беззаголовочного МСК-файла; `dotnet test BlankDemandPlanner.sln` прошел, 62/62 теста.

## v2026.07.24.1-bitrix-request-onetime-blank — 24.07.2026 13:25

- Во вкладке "Потребность" из детализации убрана колонка `Ост. до`, панель детализации расширена, а рядом с `всего деталей` добавлен показатель `дефицит` как потребность минус количество в производстве.
- В окне "Расчет материалов" кнопка экспорта переименована в `Сделать заявку Bitrix 24`, а колонка `Дата потребности заготовки` расширена, чтобы заголовок помещался целиком.
- Поиск заготовки в НСИ для расчета стал нечувствителен к регистру и ищет по наименованию, материалу, коду УТ и алиасам.
- Контекстное меню строки расчета теперь позволяет добавить заготовку для выбранной строки как разовое назначение текущего расчета; библиотека и НСИ при этом не изменяются.
- Excel-заявка формируется файлом `Заявка на закуп заготовок ЦМО от <дата> <номер>.xlsx`, содержит только листы `Заявка` и `По группе`; из заявки убрана колонка `В производстве`.
- Лист `По группе` суммирует одинаковые УТ-коды, а для строк без УТ использует IPS как код группировки; добавлены регрессионные проверки разового назначения и группировки `0,85 + 0,46 = 1,31`.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошел, 61/61 тест.

## v2026.07.23.5-stock-warehouse-groups — 23.07.2026 18:20

- Расширены правила остатков 1С: НЗП/остаток на ЦМО теперь суммируется по `44 секция НЗП (незавершенное производство)` и `Детали МУ в обработке на стороне`.
- Колонка НСИ `Остаток на складе` переименована в `Склад`; складской остаток теперь суммируется по `1 секция (ПЗМЦ) только для производственных целей`, `Покраска ТМЦ`, `ТМЦ по проекту ФРП`, `ТМЦ для проверки ОТК`, `ТМЦ для временного хранения ЦСТС`, `ТМЦ для временного хранения ЦМО`.
- Синхронизация 1С теперь передает в Python-выгрузку несколько складов для одной роли и сохраняет их в один снимок остатков.
- Обновлены статусы обновления 1С: вместо `склад 1` и `НЗП 44` показываются группы `Склад` и `НЗП/ЦМО`.
- Добавлены регрессионные проверки расчета и НСИ по новым группам складов; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.4-msk-detail-panel-layout — 23.07.2026 17:45

- Во вкладке "МСК" окно "Информация по детали" перенесено вправо и занимает половину рабочей области вкладки; добавлена прокрутка, чтобы помещалась вся детальная информация.
- Информация по детали теперь обновляется без открытия Excel: автоматически после поиска с одним результатом или точным IPS, при выборе строки и при наведении на строку таблицы.
- Добавлены обработчики наведения и выбора строк МСК, чтобы правая панель всегда показывала текущий объект.
- Добавлена регрессионная проверка автопоказа информации после поиска; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.3-msk-detail-blank-selection-fix — 23.07.2026 17:20

- Во вкладке "МСК" кнопка обновления переименована в `Обновить данные МСК`.
- Исправлено открытие файла МСК после поиска: если строка не выделилась явно, команда берет единственную найденную строку или точное совпадение по IPS.
- Нижняя панель "Информация по детали" показывает расширенные атрибуты: IPS, обозначение, наименование, вид заготовки, заготовку, материал, код УТ, норму расхода, единицу и срок.
- Таблица МСК дополнена колонками по заготовке, материалу, коду УТ, норме и сроку; атрибуты берутся из активной связи библиотеки, а при ее отсутствии - из CSV-анализа МСК.
- В "Подборе заготовок" выбор детали из выпадающего списка фиксируется в поле и больше не сбрасывается после обновления подсказок.
- При вводе первых символов в поиске детали без заготовки список вариантов раскрывается автоматически.
- Поиск заготовок по материалу стал искать по материалу, каноническому наименованию и алиасам НСИ; запрос вроде `Сталь` теперь находит подходящие позиции.
- Добавлены регрессионные тесты на фиксацию выбранной детали, поиск по материалу и расширенную карточку МСК; `dotnet test BlankDemandPlanner.sln` прошел, 59/59 тестов.

## v2026.07.23.2-msk-window-library-delete — 23.07.2026 16:30

- Исправлено удаление строк "Библиотеки" для деталей без заготовки, которые уже пришли из потребности: карточка архивируется и пропадает из списка, строки потребности отвязываются, действие можно отменить.
- Добавлена рабочая вкладка "МСК" вместо заглушки.
- Вкладка "МСК" читает Excel-файлы из `X:\19_МЕХ УЧАСТОК\База МСК\СПИСОК МСК`, извлекает IPS, обозначение, наименование и путь к исходному файлу.
- Реестр МСК сводится с библиотекой по IPS: в таблице видно, есть ли деталь в библиотеке и есть ли для нее файл МСК.
- Добавлен поиск МСК по IPS, обозначению, наименованию и имени файла; при выборе строки в нижнем окне показывается информация по детали.
- Добавлена кнопка `Открыть Excel МСК`, которая открывает исходный файл МСК через Excel/системное приложение.
- Добавлены регрессионные тесты на удаление детали без заготовки и сведение МСК с библиотекой; `dotnet test BlankDemandPlanner.sln` прошел, 57/57 тестов.

## analysis-2026.07.23-msk-library-extract — 23.07.2026

- Пересобран анализ Excel-базы МСК `X:\19_МЕХ УЧАСТОК\База МСК` для будущего переноса библиотеки в ПО.
- Итоговый файл `reports/msk_excel_analysis_20260723/msk_library_extract.xlsx` содержит основной лист `Библиотека МСК`, отдельный лист `Варианты УТ` и лист `Дубли IPS`.
- Колонка `Источник` в Excel сделана гиперссылкой на исходный файл МСК; служебные колонки `Раздел`, `Форма` и фактические поля убраны из основного листа.
- При дублировании IPS приоритет отдается актуальной папке `СПИСОК МСК`; отклоненные варианты вынесены на отдельный лист.
- Заготовка и размер разделяются, нормы рассчитываются из размеров: длинномерные заготовки по `L` переводятся в `пог. м`, листы/плиты/поковки/литье принимаются как `1 шт`.
- Уточнена раскладка для будущей библиотеки: ГОСТ материала и ГОСТ сортамента вынесены в отдельные колонки, из наименования заготовки убираются класс проката и ГОСТ.
- Вариант УТ-кода и номенклатура НСИ перенесены в основной лист `Библиотека МСК`; отдельный лист `Варианты УТ` больше не создается.
- Подбор УТ-кода теперь требует совпадения вида, материала и основного размера профиля; контрольный пример `IPS 1320756 / D80 / Сталь 40Х` выбирает `УТ000022657`, а не позицию `D330`.
- Код приложения не менялся, сборка не публиковалась.

## v2026.07.23.1-stock-code-normalization-toolbar — 23.07.2026 10:51

- Исправлено сопоставление IPS/УТ с остатками 1С: цифровые коды сравниваются как 11-значные коды 1С с ведущими нулями, поэтому `1802132` корректно совпадает с `00001802132`, а коды вида `УТ000004539` не смешиваются с IPS.
- Колонка "В производстве" во вкладке "Потребность", расчет материалов, экспорт и НСИ используют единое правило сопоставления кодов остатков 1С.
- При синхронизации 1С новый снимок остатков привязывает строки к существующим алиасам НСИ по нормализованному коду, даже если в библиотеке код был занесен без ведущих нулей.
- Из верхней панели убрана кнопка `Импорт остатков Excel`; рабочее обновление остатков выполняется кнопкой `Обновить данные 1С`.
- Верхняя панель переведена на переносимую компоновку, чтобы кнопки, статус 1С, поиск и отмена помещались в рабочую область при изменении размера окна.
- Добавлен регрессионный тест на пример `1802132 -> 00001802132`; `dotnet test BlankDemandPlanner.sln` прошел, 55/55 тестов.

## v2026.07.22.2-onec-stock-sync-sections — 22.07.2026 12:12

- Добавлена read-only синхронизация остатков из 1С через регистр `ТоварыНаСкладах` с записью результата в обычный `StockSnapshot` приложения.
- Закреплены секции: остаток ЦМО/НЗП берется только из `44 секция НЗП (незавершенное производство)`, остаток на складе для производственных целей - только из `1 секция (ПЗМЦ) только для производственных целей`.
- При запуске ПО синхронизация остатков 1С стартует в фоне; в верхней панели добавлена кнопка `Обновить данные 1С` и строка с датой/итогом последнего обновления.
- Excel-импорт остатков сохранен как резервная кнопка `Импорт остатков Excel`.
- Расчет материалов и отображение НСИ используют те же секционные правила: заготовки вычитаются только по складу 1, НЗП деталей - только по секции 44.
- Live-проверка доступа к 1С прошла: подключение к `pz-sql1.pzmc.org:1541`, выгружено 7408 строк склада 1 и 345 строк секции 44.
- Добавлен регрессионный тест секционных правил расчета; `dotnet test BlankDemandPlanner.sln` прошел, 54/54 теста.

## v2026.07.22.1-library-click-import-new-nomenclature — 22.07.2026 11:26

- Исправлен сбой в "Библиотеке" при наведении и клике по строке: таблица остается в режиме выбора ячеек, но больше не пытается программно выбирать всю строку через `SelectedItem`.
- В "Расчете материалов" строки без определенной заготовки теперь показывают в колонке "Номенклатура" значение "Новая номенклатура".
- Импорт экспортированного файла `Библиотека.xlsx` теперь понимает колонки общего файла, обновляет наименование/обозначение детали, количество и срок заготовки после ручного редактирования Excel.
- Добавлены регрессионные тесты на расчет без заготовки и цикл `Выгрузить в Excel -> отредактировать -> импортировать`; `dotnet test BlankDemandPlanner.sln` прошел, 53/53 теста.

## v2026.07.21.10-library-hover-context-crash-fix — 21.07.2026 16:00

- Исправлен сбой при открытии "Библиотеки" и наведении/появлении всплывающего списка: команды контекстного меню теперь устойчивы к служебным WPF-параметрам и не падают на `MS.Internal.NamedObject`.
- Команда "Редактировать" в библиотеке безопасно игнорирует параметры, которые не являются строкой библиотеки, и использует выбранную строку только когда она действительно доступна.
- Добавлен регрессионный тест на служебные параметры WPF для команд "Редактировать" и "Удалить"; `dotnet test BlankDemandPlanner.sln` прошел, 51/51 тест.

## v2026.07.21.9-library-cell-nsi-count-calc-button-delete-fix — 21.07.2026 15:34

- В "Библиотеке" таблица переведена в режим выделения ячеек `Cell`, чтобы при клике выделялась конкретная ячейка, а не вся строка.
- В НСИ добавлена инфострока с количеством показанных строк по аналогии с "Библиотекой".
- Во вкладку "Расчет материалов" возвращена кнопка расчета с новым названием "Произвести расчет".
- Из верхней панели рядом с "Импорт остатков по 1С" убрана кнопка "Расчет материалов".
- Удаление строки библиотеки сделано устойчивым к служебным WPF-параметрам контекстного меню, чтобы ошибка `MS.Internal.NamedObject` не приводила к падению.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошел, 50/50 тестов.

## v2026.07.21.8-library-demand-detail-filter-edit — 21.07.2026 14:35

- В окне "Расчет материалов" убрана верхняя кнопка "Расчет материалов"; запуск расчета остается через общую кнопку приложения.
- При добавлении новых IPS из потребности в библиотеку теперь учитываются только детали: строки операций, сборок, узлов, комплектов и монтажных работ не создают карточки деталей.
- Новые детали из потребности разбираются на "Обозначение" и "Наименование", если строка начинается с обозначения детали.
- В "Библиотеке" исправлено сохранение "Обозначения" и "Наименования" для карточек деталей без выбранной заготовки.
- В "Библиотеке" добавлен фильтр "Связь": все, без заготовки, с заготовкой.
- Добавлены регрессионные тесты для фильтрации деталей из потребности и сохранения наименования/обозначения без заготовки; `dotnet test BlankDemandPlanner.sln` прошел, 50/50 тестов.

## v2026.07.21.7-stable-user-data-demand-delete-library-ips — 21.07.2026 14:24

- База данных приложения перенесена в устойчивое хранилище `Данные для работы\BlankDemandPlanner.db`; при первом запуске новая база копируется из старой `%LOCALAPPDATA%\BlankDemandPlanner\BlankDemandPlanner.db`, чтобы ручные правки библиотеки и НСИ сохранялись между обновлениями ПО.
- В "Потребности" добавлена кнопка "Удалить выделенные"; можно удалить выделенные строки, действие попадает в общий механизм отмены.
- "Библиотека" перед загрузкой сверяет IPS из потребности и создает отсутствующие карточки деталей без заготовки, чтобы новые IPS появлялись в библиотеке.
- Добавлены регрессионные тесты для удаления строк потребности с отменой и для автоматического добавления отсутствующих IPS из потребности в библиотеку; `dotnet test BlankDemandPlanner.sln` прошел, 49/49 тестов.

## v2026.07.21.6-dashboard-nsi-usage-library-delete-demand-parts — 21.07.2026 14:08

- В главном виде убраны карточки "К закупке" и "Вне И-012"; оставшиеся карточки сделаны одинакового размера.
- Главные показатели оставлены привязанными к базе и обновляются при открытии главной страницы после импортов, расчетов и изменений справочников.
- Исправлено обновление окна "Применяемость" в НСИ при ячеечном выделении: клик по строке/ячейке снова показывает связанные детали.
- Исправлена ошибка удаления в "Библиотеке" из лога `MS.Internal.NamedObject`: контекстное меню удаляет одну строку, кнопка "Удалить выделенные" работает по выделенным ячейкам/строкам.
- Для строк библиотеки без заготовки добавлено полноценное удаление карточки детали с отвязкой строк потребности по `PartId`.
- В редактор НСИ для поля "Марка материала" добавлены выпадающие подсказки по уже имеющимся материалам из НСИ.
- При обновлении существующей партии потребности импорт теперь создает недостающую карточку детали по IPS и привязывает ее к строке потребности, чтобы деталь появилась в библиотеке без заготовки.

## v2026.07.21.5-selection-detail-library-delete-history-time — 21.07.2026 13:48

- Исправлено окно детализации "Потребности" после перехода на ячеечное выделение: клик по ячейке снова обновляет детализацию выбранного IPS.
- Исправлено удаление в "Библиотеке" для ячеечного выделения: команда "Удалить выделенные" собирает строки из выделенных ячеек и удаляет/архивирует выбранные связи.
- Автозаполнение редактора "Библиотеки" закреплено за выбором ячейки/строки и действием "Редактировать".
- Поведение редактора НСИ откатано: простое выделение строки обновляет применяемость, а редактор заполняется только через контекстное действие "Редактировать".
- Во вкладку "История" добавлена колонка "Дата обновления" после версии; новые записи читают дату и время из заголовка версии в `VERSION.md`.

## v2026.07.21.4-cell-selection-library-no-blank-counts

- Во всех основных таблицах выделение приведено к режиму ячеек: клик по строке выбирает конкретную ячейку, как в "Расчете материалов".
- Вкладка "Подбор заготовок" перенесена после вкладки "НСИ".
- В детализации "Потребности" в заголовок после IPS добавлено общее количество деталей в потребности по выбранному IPS.
- "Библиотека" теперь показывает детали без активной заготовки, добавлена верхняя инфострока с количеством показанных деталей и количеством деталей без заготовки по текущим фильтрам.
- Редактор "Библиотеки" заполняется при выборе/редактировании строки аналогично редактору НСИ.
- В "Расчет материалов" перед колонкой "Номенклатура" добавлена колонка "Вид заготовки".
- Список деталей без заготовки в "Подборе заготовок" синхронизирован с теми же деталями, которые теперь видны в "Библиотеке".
- Добавлены регрессионные тесты для показа деталей без заготовки в библиотеке и выбора их в подборе; `dotnet test BlankDemandPlanner.sln` прошел, 47/47 тестов.

## v2026.07.21.3-blank-selection-library-tool

- Вкладка "Подбор заготовок" дополнена выбором деталей без активной заготовки: можно набрать IPS вручную или выбрать деталь из выпадающего списка.
- По выбранной детали, найденной заготовке и норме расхода теперь можно сразу создать связь в "Библиотеке" кнопкой "Добавить в библиотеку"; старая активная связь по детали при необходимости архивируется.
- При выборе заготовки единица расхода по умолчанию подтягивается из НСИ, норму можно указать вручную в `шт` или `пог. м`.
- После импорта потребности из ПП приложение считает новые IPS, добавляет их как детали без заготовки и сообщает пользователю количество добавленных новых деталей.
- Добавлен регрессионный тест на сохранение выбранной детали и заготовки из вкладки "Подбор заготовок"; `dotnet test BlankDemandPlanner.sln` прошел, 46/46 тестов.

## v2026.07.21.2-demand-refresh-editors-blank-selection

- Исправлено обновление окна "Потребность" после последовательной загрузки файлов ПП: повторный импорт уже известного файла теперь поднимает его партию в последние, а экран показывает именно последнюю импортированную потребность.
- Добавлен регрессионный тест с несколькими файлами `*Потребность*.xlsx` с рабочего стола: последовательный импорт и повторный импорт первого файла обновляют видимый список.
- В "Библиотеке" и "НСИ" контекстное меню строки приведено к двум действиям: "Редактировать" и "Удалить"; редактирование заполняет верхний редактор выбранной строки.
- В редакторы "Библиотеки" и "НСИ" добавлена явная кнопка "Очистить".
- Подтверждена и исправлена проблема накопления разных активных заготовок для одного IPS: при сохранении или импорте новой основной заготовки старые активные связи IPS архивируются.
- Добавлена новая вкладка "Подбор заготовок": по виду, материалу и размерам детали подбирает заготовки из НСИ, которые больше или равны детали, и сортирует их от меньшего припуска к большему.
- Добавлены регрессионные тесты для обновления потребности, замены активной заготовки IPS и подбора круглой заготовки; `dotnet test BlankDemandPlanner.sln` прошел, 45/45 тестов.

## v2026.07.21.1-ips-viewer-research

- Выполнена разведка установленного ClickOnce-приложения `IPS Viewer` для будущей привязки деталей к данным IPS и PDF-чертежам.
- Найдены локальный кэш приложения, источник публикации, классы `Sql` / `ObjectIPS` / `LinkIPS` / `FileIPS` и основные таблицы IPS: `IMS_OBJECTS`, `IMS_OBJECT_ATTRS`, `IMS_RELATIONS`, `IMS_STORAGE`.
- Зафиксировано, что PDF-чертежи читаются из `IMS_STORAGE.F_FILEBODY` по `F_FILE_ID`, а список файлов объекта фильтруется через `F_ATTRIBUTE_ID=1002`.
- Рабочая строка подключения к базе IPS пока не найдена: сетевой каталог публикации недоступен на чтение, тест SQL к `pz-sql1` завершился таймаутом.
- Добавлен документ `docs/IPS_VIEWER_INTEGRATION_NOTES.md` с найденными запросами, ограничениями и рекомендуемым read-only планом внедрения.
- Код приложения не менялся, новая сборка не публиковалась.

## v2026.07.20.16-onec-stock-fast-under-30

- Быстрый запрос остатков 1С оптимизирован под оперативные ответы: по умолчанию читается только основной регистр `ТоварыНаСкладах`.
- Сверка с `ПартииТоваровНаСкладах` и `ТоварыОрганизаций` перенесена на явный флаг `--control`.
- Добавлен пакетный режим до 30 строк в одном COM-подключении: `lookup_1c_stock.cmd --items-file <файл> --warehouse <секция>`.
- Для пользовательского ввода кодов УТ добавлена нормализация, например `11445` дополнительно проверяется как `УТ000011445`.
- Так как холодный COM-старт занимает около 51 секунды, подготовлен прогретый read-only helper-сервер `stock_lookup_server.py` с запуском через `start_stock_lookup_server.cmd` и запросами через `query_stock_server.cmd`.
- Проверка прогретого helper-сервера: запрос `11445` по секции `44` отработал за 0,7 секунды.
- Проверка: Python unit-тесты интеграции прошли 4/4; компиляция новых Python-скриптов прошла; `dotnet test BlankDemandPlanner.sln` прошел 39/39.
- Быстрый путь закреплен в `AGENTS.md`, `PROJECT_CONTEXT.md`, `ONEC_STOCK_FAST_PATH.md`, README и `SESSION_HANDOFF.md`.

## v2026.07.20.15-onec-diagnostics-path-move

- Обновлен закрепленный путь диагностического контура 1С: теперь используется `C:\Users\dpd\Documents\Codex\1C_Diagnostics`.
- Быстрая команда `onec_integration\lookup_1c_stock.cmd` перенастроена на новый путь Python venv и `.env`.
- Обновлены `AGENTS.md`, `PROJECT_CONTEXT.md`, `SESSION_HANDOFF.md`, README и инструкции `ONEC_STOCK_FAST_PATH.md` / `STOCK_SYNC_IMPLEMENTATION_PLAN.md`, чтобы будущие запросы остатков сразу использовали новое расположение.
- Проверка: быстрый live-запрос остатков вернул `2`; Python-тесты интеграции прошли 4/4; `dotnet test BlankDemandPlanner.sln` прошел 39/39.

## v2026.07.20.15-demand-nsi-dashboard-calculation-fix

- Во вкладке "Потребность" детализация больше не меняется от простого перемещения мыши: панель обновляется по выбранной строке, аналогично применяемости в НСИ.
- После загрузки потребности из ПП экран принудительно очищает трекинг БД и обновляет вкладку "Потребность" и главную страницу.
- Колонка "В производстве" в потребности показывает фактический остаток НЗП перед текущей строкой, то есть уже за вычетом строк выше по тому же IPS.
- В окне "Расчет материалов" скрываются строки с нулевым количеством к заказу, а колонка "В производстве" убрана.
- В НСИ после "Остаток на ЦМО" добавлена колонка "Остаток на складе".
- На главной странице карточки показателей стали активными: нажатие открывает соответствующую вкладку.
- Добавлены регрессионные тесты для НЗП в потребности, скрытия нулевых строк расчета и раздельных остатков НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 41/41 тест.

## v2026.07.20.14-demand-details-panel

- Во вкладке "Потребность" окно детализации перестроено по образцу панели применяемости НСИ: основная таблица и детализация расположены рядом.
- Добавлена кнопка "Скрыть детализацию" / "Отобразить детализацию".
- Наведение на строку потребности продолжает обновлять детализацию по IPS.
- Добавлен регрессионный тест для переключения панели детализации; `dotnet test BlankDemandPlanner.sln` прошел, 39/39 тестов.

## v2026.07.20.13-nsi-size-demand-details

- В НСИ исправлено сохранение правки при размере, который уже отображается в формате `D... W... H... T... S... L...`; редактор теперь понимает такие размеры и не блокирует изменение из-за рассогласования формата.
- Во вкладку "Потребность" добавлено окно "Детализация IPS" в правой верхней части.
- При наведении на строку потребности окно детализации показывает все строки по тому же IPS: дату потребности, количество деталей, остаток "В производстве" до строки, списание по строке, остаток после строки, проект и № станка.
- Добавлены регрессионные тесты для сохранения НСИ с форматом размера `W60 H174 L970` и детализации повторяющегося IPS; `dotnet test BlankDemandPlanner.sln` прошел, 38/38 тестов.

## v2026.07.20.13-onec-stock-fast-path

- Закреплен быстрый read-only путь получения текущих остатков из 1С УТ через COM без Excel: `onec_integration\lookup_1c_stock.cmd`.
- Подтверждены реальные регистры для остатков: основной `ТоварыНаСкладах`, контрольные `ПартииТоваровНаСкладах` и `ТоварыОрганизаций`, ресурс `Количество`.
- Для проблем с DNS/FQDN добавлен рабочий fallback подключения `pz-sql1:1541`; учетные данные остаются во внешнем `C:\Users\dpd\Documents\Codex\1C_Diagnostics\.env`.
- Добавлены инструкции `ONEC_STOCK_FAST_PATH.md` и `STOCK_SYNC_IMPLEMENTATION_PLAN.md`, чтобы в будущем внедрить прямую синхронизацию остатков в ПО вместо Excel-импорта.
- `config.example.json` заполнен известными метаданными остатков УТ 10.3, чтобы не начинать последующие запросы с повторного обследования.
- Проверочный live-запрос для `01-P002-03.002-T63.150.SS` и секции `44` вернул остаток `2` в основном и контрольных регистрах.
- Проверка: Python-тесты интеграции прошли, 4/4; `dotnet test BlankDemandPlanner.sln` прошел, 38/38.

## v2026.07.20.12-demand-wip-lead-time-fix

- Во вкладке "Потребность" исправлена колонка "В производстве": остатки 1С теперь сопоставляются с IPS без учета ведущих нулей, например `00001799544` засчитывается для IPS `1799544`.
- Статус потребности после "уникальных IPS" показывает суммарное количество деталей по отображенным строкам.
- В НСИ объединены кнопки удаления: оставлена одна кнопка "Удалить", работающая для одной или нескольких выделенных строк.
- В библиотеку добавлено поле и колонка "Срок заготовки, дней" с базовым значением 30; значение можно менять вручную для связи детали с заготовкой.
- В расчете материалов "Дата потребности заготовки" теперь считается как дата потребности детали минус срок из библиотеки; базово 30 дней.
- Excel-выгрузка расчета использует ту же дату потребности заготовки и получила чистые русские заголовки листа "Заявка".
- В выгрузку библиотеки добавлен срок заготовки в днях.
- Добавлена миграция `20260720190000_AddBlankLeadTimeDays`; `dotnet test BlankDemandPlanner.sln` прошел, 36/36 тестов.

## v2026.07.20.11-onec-integration-foundation

- Добавлен отдельный проект `onec_integration` для безопасной интеграции с 1С:УТ 10.3: Python COM-клиент, CLI, конфиг, журналирование, обработка ошибок, тесты и инструкции.
- Добавлены шаблоны 1С-модулей для обследования метаданных и будущего HTTP-сервиса расширения без создания документов на диагностическом шаге.
- Запись документов защищена флагами `DRY_RUN`, `ALLOW_DOCUMENT_WRITE` и `ALLOW_DOCUMENT_POSTING`; по умолчанию доступны только чтение, валидация и dry-run.
- Добавлены `.env.example` и `.gitignore`, чтобы пароль 1С, логи и временные файлы не попадали в исходники.
- Выполнена локальная проверка окружения: толстый клиент 1С 8.3.27.2214 найден, но DNS-имя `pz-sql1.pzmc.org` на этой машине не разрешилось, поэтому фактическое обследование базы 1С не выполнено.
- Проверка: unit-тесты Python `python -m unittest discover onec_integration/tests` прошли, 3/3; dry-run команды комплектования и перемещения возвращают успешный предварительный результат.

## v2026.07.20.10-demand-library-nsi-edit-fix

- Повторно исправлено отображение номера станка вида `�25633`: очистка больше не портит уже исправленную букву `Р` и заменяет ошибочный символ перед цифрами после восстановления кодировки.
- Во вкладку "Потребность" добавлена колонка "В производстве"; значение считается по последнему снимку остатков 1С, где код совпадает с IPS и единица равна `шт`.
- Для нескольких строк одного IPS остаток "В производстве" распределяется сверху вниз по видимому порядку строк: верхняя строка забирает свою часть первой, следующая получает остаток.
- В библиотеке исправлено ручное добавление детали с заготовкой: выбор из выпадающего списка удерживается, автоматически заполняет код УТ, а при сохранении заготовка дополнительно находится по тексту `наименование | код`.
- В НСИ исправлено ручное редактирование заготовки: можно менять код УТ / IPS, наименование, вид, единицу, материал и размер; дубль кода ловится до сохранения.
- Убраны старые поврежденные статусные сообщения из сохранения правки НСИ.
- Добавлены регрессионные тесты для номера станка `�25633`, распределения "В производстве" по строкам IPS, сохранения библиотеки по `наименование | код` и ручной правки НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 33/33 тестов.

## v2026.07.20.9-forging-library-search-fix

- В НСИ убрана лишняя кнопка "Отобразить применяемость"; остается рабочая кнопка скрытия/отображения панели, а применяемость загружается по выбранной строке.
- В справочник видов заготовок добавлен новый тип "Поковка"; нормализация импорта распознает слово "поковка".
- В библиотеке исправлен выбор покупных заготовок: ручной выбор вида "Покупная" больше не конфликтует с автоматическим распознаванием размеров `D...`.
- Поиск заготовки в библиотеке теперь понимает формат `наименование | код`, например `Заготовка оправки ВТ40-D70-L175 | 1802132`.
- Для покупных заготовок список подсказок допускает ранее импортированные позиции с типом "Прочее" или "Не распознано", чтобы их можно было выбрать и связать с деталью.
- Добавлены регрессионные тесты для распознавания "Поковка" и поиска покупной заготовки по примеру `1802132`; `dotnet test BlankDemandPlanner.sln` прошел, 29/29 тестов.

## v2026.07.20.8-machine-nsi-stock-request-fix

- В номере станка добавлена очистка ошибочного символа `�` перед цифрами: значения вида `�25604` отображаются как `Р25604`.
- В НСИ добавлена кнопка `+` для ручного занесения заготовки в базу; форма позволяет заполнить код УТ / IPS, наименование, вид, единицу, марку, размер и ГОСТы.
- Экран "Расчет материалов" теперь показывает в заявке только количество к закупке после вычета складских остатков; полностью обеспеченные складом строки не попадают в заявку.
- Выгрузка "Расчета материалов" в Excel использует ту же логику: если склад закрывает потребность, строка не выгружается как дефицит.
- Excel-экспорт расчета и библиотеки снова приведен к нормальным русским именам файлов, листов и заголовков.
- Применяемость НСИ устойчивее сопоставляет очищенные старые строки и новые данные.
- Добавлен регрессионный тест на потребность `0,133 пог. м`, полностью покрытую складом; `dotnet test BlankDemandPlanner.sln` прошел, 27/27 тестов.

## v2026.07.20.7-material-export-library-nsi-fix

- В "Расчете материалов" восстановлена выгрузка Excel: файл сохраняется как `Расчет_материалов_<номер>.xlsx`, а если файл занят Excel, создается резервная копия с датой и временем.
- Экспорт расчета получил нормальные русские имена листов и заголовков без mojibake.
- В библиотеку добавлена кнопка `+` для ручного добавления новой строки через редактор.
- В НСИ добавлена отдельная команда "В архив" для нескольких выделенных строк; одиночные команды удаления и архива также перенаправлены на общий рабочий механизм выделения.
- Окно применяемости НСИ восстановлено: кнопка явно загружает применяемость выбранной строки, а сопоставление дополнительно учитывает алиасы и наименования заготовок.
- В списке "Вид заготовки" убран дубль "Не распознано"; старый технический тип `CustomBlank` отображается как "Прочее".
- Дополнительно очищены видимые сообщения импорта/редактирования НСИ и библиотеки от старых иероглифов.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошла, 26/26 тестов; были предупреждения NU1900 из-за недоступного `https://api.nuget.org/v3/index.json`.

## v2026.07.20.6-dashboard-popup-export-selection-fix

- На главной странице исправлены подписи разработчика и версии: старые иероглифы заменены на нормальный русский текст и текущую версию.
- В статусах библиотеки и НСИ добавлена автоматическая очистка старого mojibake-текста перед отображением.
- Заголовки всплывающих окон импорта библиотеки и НСИ восстановлены нормальным русским текстом.
- В библиотеке убрана кнопка удаления из каждой строки; удаление доступно через правую кнопку мыши по строке и через кнопку "Удалить выделенные".
- Выгрузка библиотеки больше не падает, если `Библиотека.xlsx` открыт в Excel: приложение сохраняет резервный файл `Библиотека_yyyyMMdd_HHmmss.xlsx`.
- В НСИ колонка "Остаток на ЦМО" показывает `0`, если остатка по позиции нет в последнем снимке.
- Добавлены регрессионные тесты для занятого файла выгрузки и нулевого остатка НСИ; `dotnet test BlankDemandPlanner.sln` прошел, 26/26 тестов.

## v2026.07.20.5-library-export-nsi-stock-fix

- В поисковом фильтре библиотеки по единице измерения убрана единица `кг`; остались `Все ед.`, `шт`, `пог. м`.
- Удаление строк библиотеки усилено: строка архивируется без физического удаления детали, кнопка удаления добавлена прямо в строку, а массовое удаление понимает выделенные ячейки и строки.
- Выгрузка библиотеки теперь создает в выбранной папке ожидаемый файл `Библиотека.xlsx` и заменяет предыдущий файл с таким именем.
- Сообщения выгрузки библиотеки восстановлены нормальным русским текстом.
- В НСИ колонка "Остаток факт" переименована в "Остаток на ЦМО"; колонка "Ед. остатка" убрана.
- Добавлены регрессионные тесты для фильтра единиц библиотеки, удаления строки библиотеки и имени файла выгрузки; `dotnet test BlankDemandPlanner.sln` прошел, 25/25 тестов.

## v2026.07.20.4-demand-undo-copy-calculation-fix

- Вкладка "Потребность": убрана колонка PS, добавлена колонка "IPS детали", "Серийный №" заменен на "№ станка".
- В статусе потребности рядом с количеством строк добавлен счетчик уникальных IPS.
- В потребности добавлены ручное добавление строки кнопкой `+`, сохранение ручных строк и ограничитель по дням от текущей даты потребности.
- В библиотеке из редактора единиц расхода убрана единица `кг`; удаленные/архивированные связи больше не остаются пустыми строками в списке.
- Исправлена причина повторного расчета одинаковой строки: одинаковые активные связи одной детали с одной заготовкой и единицей теперь учитываются один раз.
- В расчет материалов и основные окна импорта возвращены нормальные русские статусы без mojibake.
- Копирование из таблиц теперь выполняется без заголовка колонки.
- Таблицы переключены на выбор ячеек/заголовков строк вместо принудительного выделения всей строки.
- Добавлена кнопка "Отменить" в правом верхнем углу; хранится до 10 последних действий для ручной потребности, удаления библиотеки и ручного назначения заготовки.
- Добавлен регрессионный тест на дубли активных привязок; `dotnet test BlankDemandPlanner.sln` прошел, 22/22 тестов.
- Опубликована сборка в `publish/versions/v2026.07.20.4-demand-undo-copy-calculation-fix`.
## v2026.07.20.3-ui-text-history-settings

- Восстановлены читаемые русские значения единиц измерения и видов заготовок в библиотеке, НСИ, расчете материалов и выборе заготовок.
- Добавлена очистка старых mojibake-строк при отображении обозначений, наименований, складов и подсказок заготовок без изменения данных в базе.
- В НСИ добавлена команда "Удалить выделенные" для удаления нескольких выбранных строк; позиции с историей остатков переводятся в архив.
- В НСИ фактический нулевой остаток теперь отображается как `0`, если строка остатков реально есть.
- Вкладка "История" теперь показывает версии обновлений и краткое описание работ из `VERSION.md`.
- Вкладка "Настройки" получила изменение шрифта, размера шрифта, положения верхних кнопок, выбор иконки окна и пользовательского изображения.
- Проверка: `dotnet test BlankDemandPlanner.sln` прошла, 21/21 тестов; предупреждения NU1900 связаны с недоступностью `https://api.nuget.org/v3/index.json`.
- Опубликована сборка в `publish/versions/v2026.07.20.3-ui-text-history-settings`.
## v2026.07.20.2-update-version-workflow

- Закреплено правило: все работы по опубликованным обновлениям, сборкам и версиям ведутся в `C:\Users\dpd\Documents\Заказ заготовок для ЦМО\publish\versions`.
- Для каждой опубликованной версии должна создаваться отдельная папка `publish/versions/<version>`, совпадающая с заголовком версии в этом журнале.
- После завершения обновления ПО "Заказ заготовок ЦМО" должно открываться для пользовательской проверки.
- Обновлены проектные инструкции `AGENTS.md`, `PROJECT_CONTEXT.md` и `SESSION_HANDOFF.md`.
- Опубликована сборка в `publish/versions/v2026.07.20.2-update-version-workflow`.

## v2026.07.20.1-project-memory-recovery

- Восстановлена проектная память по работе за 2026-07-13 - 2026-07-17 на основании исходных файлов, текущих исходников, тестов, `VERSION.md`, `publish/versions` и временных логов сборки.
- Добавлен файл `RECOVERY_2026-07-13_17.md` с хронологией, источниками восстановления, ограничениями и защитным алгоритмом.
- Зафиксировано, что `.git` в проекте пустой, а команда `git` в текущем PowerShell-окружении недоступна; историю нельзя считать восстановимой через reflog/commits.
- В проектные правила добавлен обязательный протокол завершения треда: обновлять handoff, журнал версий, проверку и публикационные артефакты.

## v2026.07.17.3-ui-library-nsi-fix

- Во вкладке "Потребность" восстановлены читаемые русские статусы в верхней части экрана.
- В левом меню "Расчет материалов" перенесен сразу после "Потребность"; отдельная вкладка отчетов не добавляется и не используется.
- В библиотеке удаление работает для одной и нескольких выделенных строк: добавлена команда "Удалить выделенные", контекстное меню удаляет весь текущий выбор.
- Для НСИ добавлено аварийное восстановление: если активный справочник пуст, приложение возвращает архивные позиции, а при полном отсутствии НСИ поднимает временные строки из последнего снимка остатков.
- Исправлены отображаемые русские значения видов заготовок и единиц измерения в библиотеке/НСИ.

## v2026.07.17.2-wip-in-material-calculation

- В расчете материалов добавлена колонка "В производстве".
- Строки остатков 1С, где код совпадает с IPS детали из потребности, учитываются как НЗП.
- НЗП вычитается из количества деталей перед расчетом потребности в заготовках; при нескольких строках одного IPS НЗП распределяется последовательно и не вычитается повторно.
- Excel-лист "Заявка" дополнен колонкой "В производстве" и выгружает количество материала уже за вычетом НЗП.
- Добавлен регрессионный тест: потребность 10 деталей, НЗП 4 детали, остаток заготовки 2 шт. дает к закупке 4 шт.

## v2026.07.17.1-library-nsi-material-calculation

- В библиотеке добавлена команда удаления данных: строку можно удалить через контекстное меню правой кнопкой мыши.
- Вкладка "Остатки" убрана из левого меню; фактический остаток последнего снимка склада теперь отображается в НСИ после колонки "ГОСТ сортамента".
- "Расчет" переименован в "Расчет материалов"; таблица расчета приведена к форме заявки для снабжения: №, ПС, № станка, IPS, наименование, количество деталей, код УТ, номенклатура, ед. измерения, количество и дата потребности.
- Для строк расчета без подобранной заготовки добавлена ручная привязка заготовки из НСИ с количеством на деталь и единицей измерения.
- Экспорт расчета в Excel дополнен первым листом "Заявка" в формате для формирования закупочной потребности.

## v2026.07.16.16-demand-refresh

- Исправлено отображение ранее загруженных строк потребности, где `Наименование операции` ошибочно попадало в колонку `Наименование`, а `Проект`, `Серийный №` и `Ед. изм.` оставались пустыми.
- Повторная загрузка того же файла потребности больше не создает дубль пакета: существующий пакет обновляется по строкам и заполняет поля `Проект`, `Серийный №`, `PS`, `Наименование`, `Ед. изм.`, `Количество`, `Дата потребн.`.
- При открытии вкладки "Потребность" старый пакет с пустыми новыми полями автоматически перечитывает исходный Excel, если файл еще находится на диске.
- Для строки `IPS 1814870` из `Потребность.xlsx` теперь подтягиваются значения: `ПС200 ОДК-ПМ`, `Р24500-ВД`, `Р24500`, `КУ260305... Фланец...`, `шт`, `1`, `15.07.2026`.
- Добавлен регрессионный тест повторной загрузки файла потребности без удвоения данных.

## v2026.07.16.15-demand-view

- После команды "Загрузить потребность из ПП" приложение автоматически открывает вкладку "Потребность".
- Вкладка "Потребность" теперь показывает строки последнего загруженного пакета в составе: Проект, Серийный №, PS, Наименование, Ед. изм., Количество, Дата потребн.
- Импорт потребности читает дополнительные поля из выгрузки ПП: "Проект", "Серийный №", "Входит в станок"/PS, "Ед. изм." и "Дата потребн.".
- Исправлено автоматическое сопоставление колонок Excel: точное совпадение заголовка имеет приоритет над частичным, поэтому "Наименование" больше не подменяется колонкой "Наименование операции".
- Добавлена миграция SQLite `20260716150000_AddDemandDisplayFields` для сохранения полей Project, SerialNumber и Unit в `DemandItems`.

## v2026.07.16.14-matched-library-stock-view

- Заголовок вкладки "Библиотека" переименован в "Справочник Деталей и Норм расходов материалов и заготовок".
- Исправлен импорт файла сопоставления заказа заготовок: добавлен профиль листа "Итог" с колонками IPS, размер заготовки, вид заготовки, УТ код, наименование по 1С, материал, ед. изм. и потребность.
- Для нестандартных/поврежденных Excel-файлов добавлен резервный OpenXML-reader, если EPPlus не может открыть книгу.
- Профиль остатков 1С теперь понимает колонки "Код позиции 1С", "Наименование позиции / заготовки" и "Остаток/кол-во".
- Вкладка "Остатки" теперь показывает последний снимок "УТ остаток на дату" и таблицу выгрузки 1С УТ.

## v2026.07.16.13-library-import-export-navigation

- Импорт библиотеки перенесен во вкладку "Библиотека".
- Во вкладку "Библиотека" добавлена выгрузка текущей библиотеки в Excel.
- Верхняя команда "Импорт потребности" переименована в "Загрузить потребность из ПП" и поставлена первой.
- Верхние команды "Импорт 1С" и "Импорт остатков" объединены по интерфейсу в одну команду "Импорт остатков по 1С".
- В левом меню вкладка "Потребность" перенесена на второе место сразу после "Главная".

## v2026.07.16.12-nsi-gost-logo-fix

- Подпись на главной изменена на "Разработал Codex с участием Аракеляна А.С.".
- Логотип заменен на увеличенный черный PNG с прозрачным фоном, без темной подложки в интерфейсе.
- В редактор НСИ добавлены поля "ГОСТ материала" и "ГОСТ сортамента".
- Исправлено сохранение правки НСИ после изменения марки материала: выбранная строка сохраняется до перезагрузки списка, а ошибка сохраняется в таблицу Logs с понятным сообщением пользователю.
- Из таблицы НСИ убран столбец "Заготовка".

## v2026.07.16.11-dashboard-nsi-edit

- На главной странице добавлен фирменный блок с логотипом предприятия, фразой "Создаём, развиваем, гордимся", датой последнего обновления и подписью "Разработал Аракелян А.С. с участием ИИ".
- Карточка "Канонических заготовок" переименована в "Заготовок".
- В НСИ добавлен редактор выбранной позиции: можно изменить вид заготовки, марку материала и размер.
- При вводе размера вида `ШxВxД`, например `40x100x1030`, заготовка автоматически получает вид "Лист", если это не труба.

## v2026.07.16.10-source-name-nsi-order-logo

- Наименование заготовки в Библиотеке, выборе заготовки и НСИ теперь берется строго из выгруженного наименования 1С/НСИ (`SourceName`), а не из нормализованного имени.
- В НСИ изменен порядок колонок: Вид, Марка, Размер, Код УТ / IPS, Наименование по 1С УТ, Ед. изм., ГОСТ материала, ГОСТ сортамента, Заготовка, Источник, Обновлено.
- Колонка "Заготовка" в НСИ теперь также показывает исходное выгруженное наименование.
- Логотип в верхней панели выровнен по центру своего блока.

## v2026.07.16.9-nsi-types-usage-layout

- Добавлены виды заготовок: "Сварочный элемент", "Покупная", "Литье"; пользовательский пункт "Специальная заготовка" убран из выбора и отображения.
- Нормализация теперь относит заготовки с размерами ширина x высота x длина к виду "Лист", если это не труба.
- НСИ допускает код "УТ" или IPS в колонке "Код УТ / IPS"; резервный импорт Excel умеет подхватить IPS-код заготовки-детали с соседним наименованием.
- В окнах ввода и фильтрах добавлены явные названия полей.
- Панель "Применяемость" в НСИ получила растягиваемую ширину через разделитель между таблицей и правой панелью.
- В верхней панели оставлен только графический знак компании без текстовой части логотипа.

## v2026.07.16.8-nsi-ut-import-archive-logo

- Импорт НСИ теперь показывает понятное сообщение об итогах и включает резервный поиск УТ кода по всему листу, если Excel не имеет нормальных заголовков.
- Применяемость в НСИ привязана к выбранному УТ коду и IPS через связи библиотеки; выбор строки НСИ сделан по всей строке.
- Добавлены команды "В архив" и "Удалить" для справочника НСИ. Если позиция участвует в истории остатков, удаление переводит ее в архив.
- Добавлен логотип АО "СТП "ПЗМЦ" с сайта pzmc.org в верхнюю панель и иконку окна.
- Публикуемый файл приложения переименован в "Заказ заготовок ЦМО.exe".

## v2026.07.16.7-nsi-import-usage-fix

- Исправлен импорт НСИ из Excel без нормальных заголовков: УТ код ищется прямо в строках, наименование берется из соседней ячейки или той же строки.
- Применяемость в НСИ ищет детали не только по внутреннему идентификатору заготовки, но и по УТ коду, размеру, марке и совпадающему наименованию.
- Кнопка панели применяемости переименована в "Отобразить применяемость" / "Скрыть применяемость"; при скрытии панель освобождает место.
- В интерфейсе термин "каноническая заготовка" заменен на "заготовка".

## v2026.07.16.6-nsi-usage

- Во вкладку "НСИ" добавлена кнопка "Импортировать" для загрузки списка заготовок 1С прямо из вкладки.
- Добавлена правая боковая панель "Применяемость" с возможностью свернуть и показать.
- При выборе заготовки в НСИ справа отображаются детали из библиотеки, которые используют выбранную каноническую заготовку.

## v2026.07.16.5-nsi

- Вкладка "Нормализация НСИ" переименована в "НСИ".
- Вкладка "НСИ" показывает весь список заготовок из номенклатуры 1С: УТ код, наименование, единицу измерения, вид, размер, марку, ГОСТы, каноническую заготовку и источник.
- Добавлены фильтры по виду заготовки, размеру и марке материала, а также общий поиск по УТ коду и наименованию.

## v2026.07.16.4-library-quantity-unit

- Во вкладке "Библиотека" колонка "Количество" разделена на число и отдельную колонку "Ед. изм.".
- Редактор библиотеки принимает дробное количество через запятую: 0,1; 0,35.
- Добавлена проверка расчета дробных метров: 0,1 + 0,35 + 0,45 пог. м суммируются как 0,9 пог. м и корректно уменьшаются на складской остаток.

## v2026.07.16.3-library-filters

- Во вкладке "Библиотека" добавлены поиск по списку и фильтры по виду заготовки и единице измерения.
- Таблица библиотеки поддерживает выбор и копирование отдельных ячеек.
- Для длинномерных заготовок (круг, квадрат, трубы, уголок, швеллер, двутавр, бронзовый пруток) единица отображается как "пог. м", включая ранее импортированные связи, где была записана "шт".
- При выборе заготовки в редакторе автоматически подтягиваются УТ код и единица измерения.
- Ввод дробного количества поддерживает русскую запятую: 0,1; 0,25.
- При переходе на вкладку "Библиотека" данные автоматически подгружаются из существующей SQLite базы, без очистки накопленной библиотеки.

## v2026.07.16.2-library-edit

- Во вкладке "Библиотека" вид заготовки отображается на русском.
- Единицы измерения отображаются на русском: "шт", "пог. м".
- Добавлено редактирование существующей строки библиотеки через правую кнопку мыши -> "Редактировать".
- При редактировании выбранная строка подставляется в форму сверху, включая IPS, обозначение, наименование, заготовку, УТ код, количество, единицу и источник.

## v2026.07.16.1-library

- Вкладка "Библиотека" стала постоянным справочником применяемых материалов и заготовок.
- Добавлена ручная запись детали: IPS, обозначение, наименование, вид заготовки, заготовка, количество, единица, источник.
- УТ код подтягивается автоматически из выбранной заготовки.
- Подбор заготовки сужается по виду и введенным атрибутам, например "D60".
- Новые данные добавляются в библиотеку без удаления ранее накопленной информации.
- Старые активные связи детали не удаляются; новая ручная связь становится основной.

## v2026.07.15.1-initial-application-foundation

- Создано решение `.NET 8 / WPF / SQLite / EF Core` со слоями Core, Data, Services, Infrastructure, UI и Tests.
- Заложена доменная модель деталей, заготовок, алиасов, связей расхода, потребности, остатков, расчетов, импортов и логов.
- Добавлена начальная SQLite-схема `20260715100000_InitialCreate` с индексами для IPS, УТ/1С-кодов, дат потребности и расчетных таблиц.
- Реализованы базовые сервисы нормализации, проверки И-012, расчета потребности, конвертации единиц, поиска дублей, импорта/экспорта Excel и резервного копирования.
- Создан начальный WPF-интерфейс и первые регрессионные тесты.

Предыдущие опубликованные сборки сохранены в папке `publish` и могут использоваться для отката.
