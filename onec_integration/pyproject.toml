[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "onec-integration"
version = "2026.7.20.1"
description = "Safe 1C:Enterprise integration helper for BlankDemandPlanner"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
com = ["pywin32>=306; platform_system=='Windows'"]

[project.scripts]
onec-integration = "onec_integration.cli:main"
